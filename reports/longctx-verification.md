# 长上下文正确率修复 —— 完整验收记录

> 2026-09-18|8×910C|TP8|服务默认配置
> 修复：`BAT_TOKENS` 2048 → 8192（见 [`longctx-accuracy-fix.md`](longctx-accuracy-fix.md)）

---

## 0. 验收结论

| 验收项 | 要求 | 实测 | 判定 |
|---|---|---|---|
| 前后对比（N≥10） | 修复前失败率显著、修复后 0 | 见 §1 | ✅ |
| 8K / 32K / 128K / 256K | 全部覆盖 | 8.4K / 32.2K / 130.5K / 260.0K | ✅ |
| 真实 agent 轨迹（多工具） | 覆盖 | 两条真实 dsh 会话轨迹，各 10/10 | ✅ |
| Vision | 23/23 | **23/23** | ✅ |
| GSM8K-200 | ≈198/200 | **199/200（99.5%）** | ✅ |
| `static_kernel` 降级 | 0 次 | **0** | ✅ |
| Engram-int8 常驻 | 必须 | `engram_storage=int8` + `V41_ENGRAM_HOST_RESIDENT=1` + local-owner=fast | ✅ |
| KV > 3Mi | 交付约束 | **3,088,303**（低于 3,145,728，见 §4） | ⚠️ |

---

## 1. 前后对比（同一探针、每档 10 个内容不同的样本）

探针：把唯一事实埋在长文档 **50% 处**，只问一个答案唯一的问题，字符串精确匹配判分。

| prompt_tokens | 修复前 `BAT=2048` | 修复后 `BAT=8192` |
|---:|---:|---:|
| 10,394 | 10/10 | **10/10** |
| 20,318 | 8/10 | **10/10** |
| 40,163 | 7/10 | **10/10** |
| 60,012 | 5/10 | **10/10** |
| 79,855 | 3/10 | **10/10** |
| 149,986 | ~0%（外推） | **6/6** |
| 259,985 | ~0%（外推） | **6/6** |

**修复前合计 43/50 = 86%；修复后 62/62 = 100%。**

### 1.1 验收长度档（修复后，各 10 次）

| 目标 | 实际 prompt_tokens | 命中 |
|---:|---:|---:|
| 8K | 8,412 | **10/10** |
| 32K | 32,225 | **10/10** |
| 128K | 130,538 | **10/10** |

（256K 档：259,985 token，6/6）

### 1.2 真实 agent 轨迹

用两条**真实 dsh 会话**重建的请求（27 个工具定义、多轮、含超长工具输出）：

| 轨迹 | prompt_tokens | 修复前 | 修复后 |
|---|---:|---:|---:|
| 会话 A（40 条消息） | 111,444 | **0/3** | **10/10** |
| 会话 B（11 条消息） | 81,589 | **0/4** | **10/10** |

判据：是否发出合法的工具调用。修复后 10 次全部命中，失败签名 0 个。

### 1.3 确定性

同一 prompt（**逐字节固定**）重复 10 次 → **10/10**，且输出 token 数几乎逐次相同
（61/61/61/61/61/61/61/61/52/61）—— 退化时的不确定性一并消失。

---

## 2. 与官方 API 的对照（证明是推理栈的问题，不是模型）

同一批 prompt（逐字节相同）发给官方 `api.deepseek.com`：

| chars | prompt_tokens | 官方 | 我们（修复前） |
|---:|---:|---:|---:|
| 42,000 | 32,637 | 3/3 | 3/3 |
| 128,000 | 97,140 | 3/3 | 0/3 |
| 168,000 | 126,427 | 3/3 | 0/3 |
| 340,000 | 252,047 | 3/3 | 0/3 |

**官方 12/12；我们 0/9。** ⇒ 模型有能力，是调度粒度问题。

---

## 3. 机制

chunked prefill 把长 prompt 切成 `ceil(prompt / BAT_TOKENS)` 段依次前向。
**每段都有一次独立的"偏离"机会，误差沿后续 chunk 累积**，实测约 **2%/chunk**。

用"每 chunk 2% 独立事件"预测 vs 实测（`BAT=2048`）：

| prompt_tokens | chunk 数 | 预测 | 实测 |
|---:|---:|---:|---:|
| 10,396 | 5 | 90% | 100% |
| 20,318 | 10 | 82% | 80% |
| 40,163 | 20 | 67% | 70% |
| 60,011 | 30 | 55% | 50% |
| 79,856 | 40 | 45% | 30% |

形状与量级吻合（末端实测更低，说明偏离率还随 chunk 序号上升）。

这也解释了为什么此前找不到"阈值"：**它本来就没有阈值，是平滑下滑。**

---

## 4. 代价与已知限制

### 4.1 KV 容量

| 配置 | KV cache tokens | 说明 |
|---|---:|---|
| `BAT=2048`（旧默认） | 4,145,957 | |
| **`BAT=8192`（新默认）** | **3,088,303** | activation 峰值 0.79 → 3.21 GiB |

**低于 3Mi（3,145,728）的交付约束约 1.8%。** 本次验收按"调精度期间不设 KV 硬约束"执行；
若后续版本需要同时满足 KV>3Mi，可考虑：
* 提高总显存预算（`GPU_UTIL` 已经 0.94，余量有限）
* 评估 `BAT=4096`（chunk 数减半，KV 损失更小）—— 尚未验证是否足够

### 4.2 ★ `MAX_SEQS=64` + `BAT=8192` + `GPU_UTIL=0.94` 会 OOM

实测崩溃（ACL graph 重放时）：

```
torch.OutOfMemoryError: NPUGraph.cpp:281
Resource_Error_Insufficient_Device_Memory(EL0019)
Failed to allocate 2097152 bytes ... halStreamTaskFill failed
```

原因：`BAT=8192` 让 peak activation 涨到 3.21 GiB，而 `MAX_SEQS=64` 需要
capture 桶覆盖到 384（graph memory 1.15 GiB），两者叠加把显存吃干。

**已在 `scripts/serve_a2.sh` 里加 `[MEM-GUARD]` 提示**（只在危险组合时打印建议，
不擅自改用户配置）。实测可行组合：

| 组合 | 结果 |
|---|---|
| `MAX_SEQS=32` + `BAT=8192` + `GPU_UTIL=0.94`（**发布默认**） | ✅ 正常（本文全部数据） |
| `MAX_SEQS=64` + `BAT=8192` + `GPU_UTIL=0.90` | ✅ 启动正常、60K 检索 5/5（KV 降到 2,558,512） |
| `MAX_SEQS=64` + `BAT=8192` + `GPU_UTIL=0.94` | ❌ **OOM 崩溃** |

### 4.3 未量化项

* `BAT` 增大后**每 chunk 的 host 耗时**从 ~34 ms（2048 tok）升到 ~191 ms（8064 tok）；
  prefill 吞吐与显存峰值的净影响**尚未单独测量**。

---

## 5. 复现命令

```bash
# 长上下文检索（最灵敏；60K 就能测出退化）
python3 tests/agent_trace/longctx_retrieval.py \
    --base http://127.0.0.1:8020 --tokens 8000,32000,131072 --reps 10 \
    --out results/needle.json

# agent 形态工具调用门（含真实轨迹）
python3 tests/agent_trace/accuracy_gate.py --reps 10 \
    --arms 8k,32k,128k,256k --real <重建的会话>.json \
    --corpus data/hongloumeng.txt --temperature 0 --reasoning-effort high \
    --out results/gate.json
```

原始数据：`results/verify_final/`（本次）、`results/gate_*.json`、`results/needle_*.json`
