# 提 PR 还缺什么数据 —— 逐份草稿核对（2026-09-21 21:3x）

> 口径：**只列"缺了会影响能不能发 / 发了会不会被打回"的**。纯锦上添花的不列。
> 状态三档：**【已备】**可以直接发 / **【缺·甲】**缺数据、但你或我能补 / **【缺·乙】**缺环境、补不了只能如实标注。

---

## 0. 一句话

**四份候选里，两份可以直接发，两份各差一个明确的东西。**

| 候选 | 类型 | 能发吗 | 差什么 |
|---|---|---|---|
| **① RoPE `index_select`** | PR | ✅ **可以发** | ——（已补齐：32/32+5/5、13 单测、组合校验） |
| **② MoE mask 范围比较** | PR | ✅ **可以发** | ——（8 单测 + 21/21 符号稳定） |
| **③ 上游死 import（cc1d）** | issue | ✅ **可以发** | ——（新 main 上复核过行号） |
| **④ `cpu_binding` 挂死** | issue | ⚠️ **建议先补一格** | **缺"打完补丁后能起来"的实测**（现在只有 helper 单测 + 补丁本身） |

---

## 1. PR ① RoPE `index_select` —— 【已备】

**分支** `chiro2001:perf/rope-fused-index-select` @ **`ed5b928c`**（基于 `upstream/main` = `5fbcfaa9`）

| 应有 | 现状 |
|---|---|
| 单元测试 | ✅ **13 passed**（CPU harness + A3 容器里各跑过一次） |
| 边界矩阵（RFC [91]） | ✅ **32/32 + 5/5**：`n=0` 空批逐位一致、`n=1/8/192/2048/4096`、非连续 stride、倒序、重复位置、int32、2-D fallback、`draft_index=1..5`（10/10） |
| op 计数 / kernel 数 | ✅ **6 → 2**（每方向 3→1）；int32 由 4.00 → **3.00** |
| 双帧（eager + ACLGraph） | ✅ eager −376 µs、ACLGraph −384 µs @n=4096；**27 个计时格全为负** |
| 与在途 PR 的交互 | ✅ #16285 组合分支 `4abfa85e` + 组合校验 `ALL CHECKS PASSED` |
| lint / DCO / 标题前缀 | ✅ 已过（ruff 0.14.0 / `Signed-off-by` / `[Performance]`） |
| 提 PR 后的动作 | ⏳ 请 maintainer 打 `ready-precise`（正文末尾已写） |

**我这一轮修掉的两处自相矛盾**（原先会让 reviewer 困惑）：
1. checklist 里还写着"两个 int32 小回退（+20.7/+28.9 µs）已在 §5a 写出" —— 但**正文已改成 −17.9/−17.7**（`ed5b928c` 把 index build 提到每次调用一次）。
2. "已知边界"第 6 条还写着"§5a 里有两格更慢……那是另一个改动，**我们没有测过**" —— 但**那个改动就在本分支里**，且已实测。

⇒ 两处都改成"**更早一版曾回退，现已修掉**"，并指向 `logs/42`。**§5a 保留那段历史说明**（它是 PR 的设计理由之一）。

**唯一的"软"缺口**（不阻塞，但 reviewer 可能问）：
* 【缺·乙】**正式 CI 口径**：我们的 13 passed 是用独立驱动器跑的（容器 vLLM 比分支旧，`conftest` 在 `adapt_patch()` 就死）。⇒ 上游 CI 会跑，**但我们不能声称"CI 已过"**。正文已如实写明。

---

## 2. PR ② MoE mask 范围比较 —— 【已备】

**分支** `chiro2001:perf/moe-contiguous-expert-map` @ **`206d39c9`**（基于 `5fbcfaa9`）

| 应有 | 现状 |
|---|---|
| 单元测试 | ✅ **+265 行**（range 等价 / EPLB 回退 / 静态 expert_map / 默认不评估 / 布局缓存命中与失效） |
| 性能证据（**图口径**） | ✅ **ACLGraph 内 21/21 次更快**，中位 **−10.7…−12.9 µs**；**eager 21/21 次更慢** —— 两个口径都给，并说明为何按图口径评 |
| `torch.equal` 等价 | ✅ 生产形状逐元素一致 |
| 与在途 PR 的重叠 | ✅ 与 #14933 无重叠（一个改 `WithMC2`、一个改 `WithAllGather`） |
| lint / DCO / 标题 | ✅ |

**这一轮修的**：草稿头部还写着旧 SHA `3a0c48c0` ⇒ 改成 `206d39c9` + 基线 `5fbcfaa9`。

**软缺口**（同 ①，不阻塞）：
* 【缺·乙】**W8A8 无数据**：本 PR 是**量化无关**的（只改比较方式），我们的实测都在 W4A8 上。正文已明确"quantization-agnostic"并把归属挂在 RFC [63] 的 **W8A8** 条目下 —— **但 reviewer 可能要求 W8A8 上一个数**。我们没有 W8A8 权重。
* 【缺·乙】**EP>8 / 多节点**未测 —— 正文已写清适用范围（EP8 单节点）。

---

## 3. issue ③ 上游死 import —— 【已备】

| 应有 | 现状 |
|---|---|
| 引用的行号准确 | ✅ **2026-09-21 在新 main（`5fbcfaa9`）上复核**：`patch_triton.py:326`（import）、`:332`（warning 记录）、line 57（PyTorch 绑定）**全部仍然准确** |
| 单卡复现 | ✅ `logs/raw/22-cc1d-repro/` |
| 不指控、只问 | ✅ 措辞是 "which of these is intended" |
| 基线 SHA | ✅ 已从 `c173a64a` 更新为 `5fbcfaa9`（并注明首次观察版本） |

⇒ **零缺口。**

---

## 4. issue ④ `cpu_binding` 挂死 —— ⚠️ **建议先补一格**

**这是唯一还差实测的一份。**

| 应有 | 现状 |
|---|---|
| 现象 + 时间线 | ✅ 8 卡起服 35 min 超时、8 条 `[migrate]` 行、两个 `migratepages` 100% CPU |
| "不是慢、是不收敛"的判据 | ✅ 3 分钟采样三点零进展（`MemFree` Δ+24 kB / `AnonPages` Δ−56 kB / `FilePages` Δ0） |
| 两层根因 | ✅ ① `bind_memory()` 不查目标节点空闲；② **`execute_command()` 的 timeout 分支本身没有 timeout**（`p.kill()` 后那次 `communicate()` 无界） |
| 与机器状态的互证 | ✅ `logs/38`（12:57，远早于本次）已记录 node6/node7 只剩 ~0.3 GB |
| **补丁** | ✅ `pr/patches/cpu-binding-hang-fix.patch`（103 行，两处改动，`patch -p1` 干净应用 + `py_compile` 通过） |
| helper 验证 | ✅ 在**出问题的那个容器里**实测：`node0 37.2 / node4 0.3 / node5 13.7 / node6 0.9 / node7 0.3 GiB`，不存在返回 None |
| **★ 端到端实测** | ❌ **缺** —— 没有"打完补丁 + `CPU_BIND=1` ⇒ 服务真的起来了"的证据 |

### 为什么这一格重要
现在读者只能看到"我们声称补丁能修" + "helper 单测过了"。而**上游最想看的是**：
> "按你们的补丁改完，8 卡起服真的不再挂死了吗？"

有这一格，这份草稿就从"**分析 + 建议**"升级成"**分析 + 已验证的修复**"。

### 怎么补（成本很低）
8–15 现在**空着**（`dsv41-a3` 保持 `Exited`）。在**影子包**里（`shadow-pkg/patches/files/cpu_binding.py` 已经是补丁版）跑一臂：

```bash
# 期望：不再挂死；日志里出现 [migrate] ... skipped: the node has X GiB free but this rank holds Y GiB
# 且 [migrate] 那 8 条里，装不下的那几条变成 WARN 而不是阻塞
NAME=abl-cbfix PORT=8040 CPU_BIND=1 DEVS="8 9 10 11 12 13 14 15" ... bash run_test.sh
```

**预计 15–20 分钟**（若仍挂死则 35 min 超时 —— 那也是结果，会写进草稿）。

---

## 5. 补不了的（写清"为什么"，不用相邻数字顶替）

| 项 | 影响哪份 | 为什么补不了 |
|---|---|---|
| **W8A8 任何数字** | PR ① ②、RFC 材料 | 手上只有 W4A8 权重 |
| **A5 平台** | `docs/KV-CACHE-ACCOUNTING.md` 的 8-bit 结论 | 没有升腾 950 |
| **EP=16/32 / 多节点 / DP** | PR ② | 只有单节点 8 卡 |
| **正式 CI（上游 runner）** | PR ① ② | 只有他们能触发 |
| **A2 上的 DRAM 取回带宽 / ×1.7 记账系数** | `docs/KV-CACHE-ACCOUNTING.md` §7 | 我连不上 A2；现在是 A3（HCCS）的数据 |

---

## 6. 当前正在跑的（会补掉两格）

| 子代理 | 在补什么 | 预计 |
|---|---|---|
| **D_off8** | **DSV4.1 + DRAM 卸载能不能生效**（8 卡） | ~1.5 h ⇒ `logs/46` |
| （待你点头） | **issue ④ 的端到端实测**（补丁 + `CPU_BIND=1`） | 15–20 min |

> 这两格补完，**四份候选就全部是"数据齐、可发"的状态**。
