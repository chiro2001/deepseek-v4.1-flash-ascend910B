# CORRECTNESS_STATUS.md —— 精度 / 数值确定性的最新结论（v4 新增）

> 2026-09-16 21:00 CST｜**正确性线 A3-node2 chips 8-15（容器 `dsv41-a22-correct`）+ 主性能线 A3-node1**
> 本文件是**独立的一份交付级结论**：不看它就会把 v3 的旧假设当成事实。
> 逐条注明来源（报告章节 / 原始日志路径）。**所有数字都 SSH 到 A3-node2 核对过原文**。

---

## 0. 五条结论（TL;DR）

| # | 结论 | 与 v3 的关系 |
|---|---|---|
| 1 | **不存在 ctx 阈值**。确定性是**每次测量批次的抽签**，与 ctx 无关 | v3 之前流传的"≤16384 确定"**作废** |
| 2 | **draft 让 prefill 数值确定性下降约 5 倍**（`SPEC=0` clean **0.50/0.50/0.5625** vs `SPEC=1` **0.10**；第三次是**全新容器**，双侧 Fisher **p=0.0042**） | v3 未量化 |
| 3 | 但 **clean（数值确定）与 coherent（文本不退化）是两条独立的线**（2×2 `[[4,1],[4,1]]`） | 新增 |
| 4 | **"减少 forward 数"这条方向已被算术否证**（43/40=1.075 不可能产生 5 倍差） | 新增（排除一条省算力的思路） |
| 5 | 已验精度门：**Vision 23/23、GSM8K-200 = 198/200 / 199/200 / 197/200**；`HCCL_DETERMINISTIC=true` 掉到 **91/100**（仅诊断） | v3 只知道 197–199，不知道代价 |

---

## 1. 判据（**先统一口径，否则数字互相打架**）

### 1.1 `clean`（数值确定性）—— 正确性线的标准判据

同一 prompt 连发 N 次、`max_tokens=1`、`logprobs=1`，抓**同一 token 首位置 logprob**：

```
clean ⟺ (uniq_top1 == 1)  AND  (n_distinct_logprob_values == 1)
```

两个条件**互相独立**，缺一不可：前者抓"top-1 token 翻转"，后者抓"同 token 的 logprob 逐位漂移"。
**每行都必须同时打印这两个值。** 来源：`A3-node2:wt-graph/reports/correctness-line.md` §P0-1b.7。

> ⚠️ **判据自身的两次自我修正**（必须知道，否则会重犯）：
> 第一版只用"最常见 token 的 logprob 极差（spread）" ⇒ **假性干净**：若 6 发产生 5 种不同 top-1，
> 最常见那个可能只有 2 个样本，极差自然为 0。实测在 `MOE_ZERO` 臂看到过 `uniq_top1=5` 而 `spread=0`。
> ⇒ **任何统计/判据工具都必须先用已知答案自检**（本项目已栽两次，第二次是 `fisher()`）。

### 1.2 `coherent`（文本不退化）

`uniq2 = len(set(bigrams)) / len(bigrams)`（本包 `tests/multibatch/multibatch_gate.py` 同款实现）。
`uniq2 ≥ 0.5` 记连贯，`< 0.5` 记复读（连续重复文本的 bigram 去重率会掉到 0.2–0.3）。

### 1.3 `spread`（prefill 数值确定性）

`spread = 同一 token 首位置 logprob 极差（单位 nats）`，`spread == 0` ⇒ prefill 逐位确定。
比"输出文本是否相同"灵敏得多（文本相同也可能 logprob 已漂移）。

---

## 2. 结论 ①：**不存在 ctx 阈值** —— 确定性是每次批次的抽签

**来源**：`correctness-line.md` §P0-1b（"`strict` 的「ctx 阈值」**不存在**"），原始日志
`A3-node2:/tmp/strict_thr.log`、`/tmp/strict_bisect.log`、`/tmp/strict_ctl.log`。

口径：新容器、`HCCL_DETERMINISTIC=strict`、`GRAPH=1`、8 发/点、真权重 `…-qrot-mtpq`。

| 测量 | 16384 | 20480 | 21504 | 24576 | 131072 |
|---|---|---|---|---|---|
| 正序（会话 1） | **0.000** | **0.000** | 0.805 | 0.795 | 2.079 |
| 逆序（会话 2，新容器） | **0.000** | **0.568** | 0.344 | 0.908 | 1.700 |
| **重复 21504 rep1/2/3** | – | – | **0.913 / 0.000 / 1.435** | – | – |
| 重复 20480 rep1/2/3 | – | **0.000 / 0.873 / 0.509** | – | – | – |
| 正序重扫（会话 2 末） | **0.427** | 0.774 | 0.079 | – | – |

**⇒ 三条硬结论**：

1. **同一 ctx（21504）在同一会话内三次重复 = `0.913 / 0.000 / 1.435`**
   ⇒ spread 是否为 0 是**每次测量批次之间的抽签**，与 ctx 无关。
2. **16384 也会翻转**（会话 1 的 0.000 → 会话 2 正序重扫的 0.427）
   ⇒ 此前"`strict` 让 ≤18432 完全确定"**只对该次测量成立**，不能作为保证。
3. 「(20480, 21504] 阈值」是**正序时序造成的假象**，已作废。**重复测量**才是判决性实验。

**弱规律（保留）**：spread=0 的出现率 16384 = 2/3、20480 = 2/6、21504 = 1/6
⇒ 小 ctx 更容易抽到干净批次，但**没有 ctx 受保证**。

**机制读数**：污染源是一个**状态型 buffer/UB**，其"是否干净"取决于该会话/批次此前的分配历史
（与"长度扫描里偏离率随 chunk 数单调上升、每 chunk ~2% 独立事件"一致）。

### 2.1 对交付口径的影响（**重要**）

> 性能线 8K/12K 的 A/B **建立在"≤16384 确定"上 —— 该前提不成立**。
> 建议协议：**每个测量会话开跑前先做确定性自检**（同一 prompt 8 发看 spread），
> **只有自检 spread=0（且 `uniq_top1==1`、`n_distinct_lp==1`）的会话才接受该会话的 A 数据**；
> 否则重起服。这解释了为什么同一配置的 8 发 A 会跨会话差 2.3 倍（见 EXPECTED_PERF.md §3）。

---

## 3. 结论 ②：**draft 让 prefill 数值确定性下降约 5 倍**（v4 的关键正面结果）

**来源**：`correctness-line.md` §P0-1b.9；原始日志
`A3-node2:/tmp/spec0.log`（20:16）、`/tmp/spec0_rep2.log`（20:41）、`/tmp/freq3.log`（19:56–20:04）。

| 臂 | 会话 | clean 轮数 | clean 率 |
|---|---|---|---|
| **`SPEC=0`**（无投机解码） | #1（第一次测量） | **5/10** | **0.50** |
| **`SPEC=0`** | #2（同一会话） | **5/10** | **0.50** |
| **`SPEC=0`** | #3（延长到 16 轮） | **9/16** | **0.5625** |
| **`SPEC=1`**（有 draft） | A 臂 2/10 + A2 臂 0/10（同一会话） | **2/20** | **0.10** |

（ctx = 21504，真权重，判据见 §1.1。`SPEC` **不能运行时切换** ⇒ 只能跨会话比较。）

**⇒ 合并三次 `SPEC=0`：19/36 = 52.8% vs `SPEC=1` 2/20 = 10.0%，约 5 倍差。**

> **来源精度（我核过原文，逐条说清）**：
> * 前两行（5/10、5/10）在报告 `correctness-line.md` §9 的表里（第 365–368 行）；
> * **第三行 9/16 只在原始日志 `A3-node2:/tmp/spec0_rep2.log`（mtime 20:41）里**——
>   报告 §9 写于 20:36，当时还只有两次测量，**所以"0.5625"这个数字必须引用日志、不能引用报告表**；
> * `SPEC=1` 的 2/20 在报告 §9 表内（第 369 行）。

### 3.1 统计检验（**p 值必须以原文为准**）

`correctness-line.md:371` 原文：

> **Fisher 单侧 p = 0.0256**（反方向 p = 0.998）⇒ 显著。

（该值对应的表是 `[[5,5],[2,18]]`，即只用第一次 `SPEC=0` 的 5/10 对比 `SPEC=1` 的 2/20。）

⚠️ **与本轮 brief 的差异（已核对原文，按原文写）**：brief 里写"Fisher **p≈0.0026**"，
但 `A3-node2` 的 `correctness-line.md` 原文是 **0.0256**，且该值在 `session-attractor-and-clean-rate.md`
§6.4 里被写作"此前报的是 0.0026（单侧）"——**两个数字都不是原文可复算的口径**（见下表）。
本包一律引用**可复算**的值，并附 `tools/fisher_recheck.py`（含 4 个自检用例，其中一个是
"相同表必须 p=1"这个我们踩过的坑）：

| 表 | `p_right`（单侧，"SPEC0 更好"） | `p_two`（R `fisher.test` 默认） | 出处 |
|---|---|---|---|
| `[[5,5],[2,18]]`（SPEC0 第一次 5/10 vs SPEC1 2/20） | **0.0256** | 0.0256 | `correctness-line.md:371` 的"单侧"= 本行 `p_right` |
| `[[9,7],[2,18]]`（SPEC0 第三次 9/16 vs SPEC1 2/20） | 0.0039 | **0.0042** | `session-attractor-and-clean-rate.md` §6.4 的 0.0042 = `p_two` |
| `[[19,17],[2,18]]`（合并三次 SPEC0 19/36 vs SPEC1 2/20） | 0.0013 | 0.0016 | **本包复算**（前两行之外的新口径，未在任何原报告出现） |

### 3.1.1 ★ 那段被引用为 0.0026 的测量的**准确表述**（主 Agent 2026-09-16 21:1x 确认，逐字采用）

> 该 0.50 vs 0.10 的两组测量都在**同一会话**内（报告自己 §9 的 ⚠️ 已注明"跨会话复现未做"）。
> 后续子代理做了跨会话复现：`SPEC=0` 第三次（**全新容器**）得 **9/16 = 0.5625**，与 `SPEC=1` 的 2/20
> 双侧 Fisher **p=0.0042**（单侧约 0.0026）。三次 `SPEC=0` 测量 0.50 / 0.50 / 0.5625 一致。

**原始证据**：`reports/session-attractor-and-clean-rate.md` §6.4 的 `[[9,7],[2,18]] → 0.0042`（双侧；
本包 `tools/fisher_recheck.py` 对同一表复算：`p_right`=0.0039、`p_two`=0.0042 ⇒ 与原文一致）。
第三次测量的原始日志：`A3-node2:/tmp/spec0_rep2.log`（20:41，`SPEC0_rep2: clean 9/16`）。

⇒ 所以"p≈0.0026"这个数字**只在"单侧 ≈ 0.0042 的双侧"这个意义下成立**；
引用时必须同时给出表（`[[9,7],[2,18]]`）与单侧/双侧口径。本包统一写：
**双侧 p=0.0042（单侧约 0.0026）**，并附可复算脚本。

**结论不变：`SPEC=0` 的 clean 率显著高于 `SPEC=1`（约 5 倍）。**

### 3.2 限制（**不能过度外推**）

* `SPEC=0` 的三次测量中，#1/#2 在**同一会话**（强 intra-session 证据），#3 是延长重测；
  **跨容器复现只做了 1 次**（"三次含一次全新容器"）。
* `SPEC=0` 时**没有 draft ⇒ `A`/`pos` 不可用**；`ms/step = 36.1–36.3`（用 `iteration_tokens_total_count` 计步）
  ⇒ **关掉 draft 会让 128K 慢 ~6 ms/step**，这是"用性能换确定性"的明确代价。

---

## 4. 结论 ③：**clean 与 coherent 是两条独立的线**

**来源**：`correctness-line.md` §P0-1b.10；探针 `wt-graph/scripts/clean_vs_quality.py`；
原始日志 `A3-node2:/tmp/cvq2.log`（20:25–20:31，`SPEC=0`，ctx=21504，10 轮，
每轮先做 6 发 clean 判定，再跑一次 256-token 生成测 `uniq2`）。

```
                  uniq2>=0.5(连贯)   uniq2<0.5(复读)
  clean 批次            4                 1
  dirty 批次            4                 1
```

**⇒ 2×2 完全相同 ⇒ `clean` 与 `coherent` 统计独立**（`a/(a+b) = c/(c+d)`）。
clean 轮出现在第 1/3/4/6/9 轮（不集中），**无"越跑越干净/越脏"趋势**。

**⇒ 交付含义：这两条线必须分头打、分开报。**
把"数值确定"当成"文本不退化"的替身，或反过来，都会得出错误结论。

---

## 5. 结论 ④：**"减少 forward 数"这条方向已被算术否证**

若污染按"**每 forward 独立累加**"，则 `SPEC=1` / `SPEC=0` 的 clean 率比应约等于其
**forward 数之比**。实测：

```
forward 数比 = 43/40 = 1.075      实测 clean 率比 = 0.50 / 0.10 = 5
⇒ 需要 1.075^k = 5  ⇒ k = ln5 / ln1.075 ≈ 22.4
```

**k≈22.4 荒谬**（forward 数只多 7.5%）⇒ **`SPEC` 的影响不是线性累加**，
更像"draft 的存在改变了内存池/UB 残留模式"（又一个吸引子效应）。

⇒ **不要再走"少跑几次 forward 来提高确定性"这条路**（省算力能提确定性）。

---

## 6. 结论 ⑤：已验精度门 + 唯一能"救"确定性的开关及其代价

### 6.1 精度验收矩阵（真权重，**正确性线独立复测**）

来源：`correctness-line.md` §P1（口径 `MAX_SEQS=1 SP_TOKENS=5 MOE_AG=1 FUSED_MC2=1 MULTISTREAM=0`；
GSM8K-100 `--mode chat --conc 4` + Vision 23 例）。判据：**相对基线掉 >2 题即不可用**（基线 ±1 噪声）。

| # | 改动 | 开关 | GSM8K-100 | Vision | 判定 |
|---|---|---|---|---|---|
| 1 | 基线 A（stock） | – | **100/100** | **23/23** | 基线 |
| 2 | 基线 B（复测） | – | **100/100** | **23/23** | 噪声 0 |
| 3 | moe-mask-range | `V41_MOE_MASK_RANGE=1` | **100/100** | **23/23** | ✅ 通过（Δ=0） |
| 4 | qli-no-candidate | `V41_QLI_NO_CANDIDATE=1` | **99/100** | **23/23** | ✅ 通过（Δ=−1，±1 噪声内） |
| 5 | HCCL 确定性（低精度固定序） | `HCCL_DETERMINISTIC=true` | **91/100** | 23/23 | ❌ **否决**（Δ=−9；9 个错题全"给出答案但算错"= 真实数值退化） |
| 6 | HCCL 确定性（高精度固定序） | `HCCL_DETERMINISTIC=strict` | **100/100** | **23/23** | ✅ 通过（且 ≤18432 逐位确定，**但见 §2 的"抽签"更正**） |

### 6.2 GSM8K-200 三次（真机 chat 模式）

| 会话 | 结果 | 原始日志（A3-node2） |
|---|---|---|
| `gsm_gate0` | **198/200（99.0%）** | `logs/perf/gsm_gate0.log:16` |
| `gsm_lo_on` | **199/200（99.5%）** | `logs/perf/gsm_lo_on.log:16` |
| `gsm_qrot` | **197/200（98.5%）** | `logs/perf/gsm_qrot.log:16` |

（`reports/cannbot-sweep-final-verdict.md:22` 汇总口径写 "GSM8K 198/200"。
brief 说的"198/200（两轮）"= 上表前两行；**本包按三次原始日志写，全部 ≥197**。）

Vision 23/23 在 §6.1 的 5 个臂里重复出现（同一份官方 23 例）。

### 6.3 `HCCL_DETERMINISTIC` 的交付结论

```
true   → 数值退化（GSM8K 91/100）  ⇒ **绝对不能进交付**，只能当诊断工具
strict → 保精度（100/100）          ⇒ 可交付的"确定性"配置，但只能保证"当次批次"干净（§2）
写 1   → Config_Error_Invalid_Environment_Variable(EI0001) 起服失败（合法值只 true/false/strict）
```

本包 `scripts/serve_a2.sh` 的 `HCCL_DET` 开关**默认不传**（= 与 v3/历史口径一致），
只在排查时用。**默认交付配置不开任何 HCCL 确定性开关。**

---

## 7. 已证伪的假设（**不要重复这些实验**）

来源：`correctness-line.md` §P0-1.5（全部为**离线单算子、逐位比较**）。

| 假设 | 判定 | 证据（摘要） |
|---|---|---|
| QLI 块级 top-k 用 UB 残留 | **证伪** | 真实几何（S2=131072/blocks=2048/topk=512 + 注入整块同分造边界并列）下 mode=1/3 各 20 次**逐位相同** |
| MoE `unpermute` 原子加 | **证伪** | 相同输入 20 次逐位相同、交替输入两轮逐位一致 |
| 候选路径是 128K 的源 | **证伪** | `CAND_MODE=3` @128K/4tok = **4/4 不同** |
| DSpark / 投机解码是主源 | **证伪** | `SPEC=0` 仍 6/12 种输出（但**降低 clean 率**，见 §3） |
| KV 页 / 缓冲跨请求残留（历史依赖） | **证伪** | 插入不同长度/内容的干扰后无系统性偏移 |
| chunk 数 / 竞态窗口（DSA aux 流 overlap） | **证伪** | `DSA_OVERLAP=0 + HCCL_DET=true` @128K = 6/8 不同 |
| 捕获期问题（首发跑偏） | **证伪** | `HCCL_DET=true` 128K 跑偏发次是 #3/#6/#8（随机分布） |

**仍然未定位**：128K 的"第三源"。`HCCL_DET=true + CAND_MODE=3` 下
16385/16640/18432/32768 全部 = 0.000，但 **24576 = 0.261、131072 = 1.301~2.08 仍有残差**
⇒ 与候选路径、HCCL 归约顺序**都无关**。

---

## 8. 与性能线结论的对齐（**两条线互相更正了什么**）

| 主题 | 性能线（A3-node1） | 正确性线（A3-node2） | v4 的统一口径 |
|---|---|---|---|
| A 的高低 | 三吸引子（steep 15.3% / flat 7.4% / shallow 77.3%，EXPECTED_PERF §2） | 128K 的 A 中位 1.69–2.47，历史"优"模式 3.493 未复现 | **A 只当"装对没装对"的锚点**，不当绩效指标（EXPECTED_PERF §3） |
| "干净"的定义 | `pos0 ≥ 0.8`（请求级，平均 20–50 步） | `uniq_top1==1 且 n_distinct_lp==1`（单步 top-1 logprob） | 两者**粒度不同**，各自标注；不能说"等价" |
| 110 tok/s | 163 发里可交付的只有 1 发 | 依赖的 A≥3.49 **目前不成立** | **110 tok/s 交付结论：不成立**（见 EXPECTED_PERF §2.3/§2.4） |
| 交付口径 | p42 = **128K × 256 token**（steps 73–94） | 早期探针用 64 token（steps 22–45） | **统一 256-token 口径**；pos1–4 窗口差 2–3 倍，**不能混比** |

---

## 9. 交付给 A2 的**可执行**建议（按优先级）

1. **跑之前先做确定性自检**：同一 prompt 8 发，打印 `uniq_top1` / `n_distinct_lp`；
   只有 `1/1` 的会话才接受该会话的 A/形态数据。否则先重起服。（§2.1）
2. **A/文本两条线都要报**：A 报 (clean-rate, ms/step)；文本报 `uniq2`。（§4）
3. **不要打开 `HCCL_DETERMINISTIC=true`**；要确定性就用 `strict` 并知道它只保证"当次批次"。绝不写 `1`。（§6.3）
4. **不要用 `MOE_ZERO` / `MOE_NONFINITE` 当修复**（负结果，见 README「别踩坑」表）。
5. **多轮对话 / 混合 prefill+decode 必须用 `MODE=prod` 跑一遍**（EXPECTED_PERF §7）——
   这是 v3 完全没覆盖的形态。
6. **统计工具先自检**：本包 `tools/fisher_recheck.py` 内置 3 个教科书用例，任何自写检验器都照此做。（§1.1）

---

## 10. 来源清单（逐条可核）

| 内容 | 路径 |
|---|---|
| 正确性线主报告（426 行，含 §P0-1b / §P0-1 / §P1） | `A3-node2:~/projects/dsv41-workspaces/wt-graph/reports/correctness-line.md` |
| SPEC 臂原始日志 | `A3-node2:/tmp/spec0.log`、`/tmp/spec0_rep2.log`、`/tmp/freq3.log` |
| clean×coherent 2×2 原始日志 | `A3-node2:/tmp/cvq2.log` |
| ctx 阈值否证原始日志 | `A3-node2:/tmp/strict_thr.log`、`/tmp/strict_bisect.log`、`/tmp/strict_ctl.log` |
| 精度矩阵原始数据 | `A3-node2:wt-graph/logs/corr/acc_{maskA,maskB,qliB,hccldet,strict}/`、`logs/perf/gsm_*.log` |
| A 的形态 / clean-rate（性能线） | `reports/a-basin-and-acceptance-shape.md`、`reports/session-attractor-and-clean-rate.md`（**逐字复制在本包 `reports/`**） |
| DSpark 入图负控 | `reports/draft-graph-negative-control.md`（**逐字复制在本包 `reports/`**） |
