# A2 上线清单（DRAM 卸载 + KV8）

> 2026-09-22 01:2x 起草。**状态：待 A2 探测结果**（`scripts/a2_probe.sh` / `a2_pinned_probe.sh`）。
> 本文是**可直接执行的清单**：每步都有前提、命令、判据、回滚。

---

## 0. 目标

**把内网 A2（8×910B3）用起来：长上下文/多并发不掉服务质量。**
两条线：

* **线 1 · DRAM KV 卸载**：KV 被踢出 HBM 后能**取回**，不必全量重 prefill；
* **线 2 · KV8**：long-KV 从 BF16 存成 INT8，容量 ×1.84。

---

## 1. 前置：A2 上的两条探测（**都必须先做，且都不占卡**）

### 1.1 `a2_probe.sh` —— 环境与 pinned 能力

```bash
# 在 A2 的容器里（需要能看到 /dev/davinci* 与 libascendcl.so）
bash a2_probe.sh              # 或 --no-npu 只看文件系统部分
```

**看三格**：① `host_mem_pool`（预期 0）；② `aclrtMallocHost` 逐档；③ `torch_npu` `pin_memory` 逐档。

### 1.2 ★ `a2_pinned_probe.sh` —— 候选 β 的生死判据

```bash
A2_CONTAINER=<你的容器名> bash agents/P1_pinned/scripts/a2_pinned_probe.sh
```

**这一条决定 A2 用哪个 API 起池子**（脚本末尾自带 DECISION 判读）：

| 探测结果 | 结论 |
|---|---|
| `aclrtHostRegister` 成功且**拷贝逐字节一致** | ✅ **走候选 β**（普通 host 内存 + 注册）—— `OFFLOAD_NPU_WORKER_PATCH=1 NPU_OFFLOAD_HOST_MEM=registered` |
| 注册失败 / 拷贝不一致 | ⚠️ 回到 `pin_memory` 路径，但**池子上限要重新量**（`host_mem_pool=0` 是风险） |

> **已知风险**（`logs/014` §4）：A2 的 `host_mem_pool=0`，且 **Engram 206 GiB 注册曾失败**
> ⇒ β 在 A2 上**不是自动成立**，必须先探测。

---

## 2. 线 1：DRAM 卸载上线

### 2.1 需要的两个补丁（都已备好）

| 补丁 | 位置 | 作用 |
|---|---|---|
| **D2 的 scheduler 补丁** | `agents/D2_offload/patches/offload_dsv41/scheduler.py`（md5 `0302fab4…`） | 把 `state` 组从**存/查两侧**排除（这是"能存取回"的**决定项**） |
| **P1 的池子补丁** | `agents/P1_pinned/patches/offload_dsv41/cpu_npu.py` | 池子改用 `aclrtHostRegister`，绕开 `aclrtMallocHost` |

挂载开关：`OFFLOAD_SCHED_PATCH=1` + `OFFLOAD_NPU_WORKER_PATCH=1` + `NPU_OFFLOAD_HOST_MEM=registered`。
**回滚**：任一开关置 0 即完全回到现状；`registered` 注册失败会**自动回落 `pinned`**。

### 2.2 起服参数

```bash
# ★ 见下方更正：宿主实占 = 记账值 x6.945 ⇒ 48 GiB 记账 ≈ 333 GiB 宿主
TAG=a2-dram48 \
ENGRAM=0 \
OFFLOAD_GB=48 \
PREFIX_MATCH_UNIT=32 \
OFFLOAD_SCHED_PATCH=1 \
OFFLOAD_NPU_WORKER_PATCH=1 \
NPU_OFFLOAD_HOST_MEM=registered \
BAT_TOKENS=2048 MAX_SEQS=4 GPU_UTIL=0.90 \
bash scripts/serve_a2.sh
```

| 参数 | 值 | 依据 |
|---|---|---|
| `OFFLOAD_GB` | **32–48**（起步）；长上下文另算 | ★ **`logs/016` 实测更正**：宿主实占 = 记账值 **×6.945**（不是 1.7×）⇒ **32 GiB 记账 = 222 GiB 宿主**。**A2 余量 442 GiB ⇒ 记账值别超 ~48 GiB**（= 333 GiB 宿主） |
| `PREFIX_MATCH_UNIT=32` | **必须** | 不加就撞 `tokens_per_block=32 % tokens_per_hash=128`（`logs/001` §4.1 / `logs/010`） |
| `blocks_per_chunk` | **8** | `logs/045` §5.2：太大（64）会**静默零收益** |
| `--kv-cache-memory-bytes` | ≥ `max_model_len × kv_per_token` | `logs/045` §5.3：DRAM 层**不放宽**单请求最长上下文 |
| `--enable-prefix-caching` | **必须** | 卸载层与 prefix cache 是配套的 |

### 2.3 ★ 四条判据（**全中才算上线**）

| # | 判据 | 阈值 |
|---|---|---|
| ① | `BlockStored(medium="CPU")` | > 0 |
| ② | **`kv_offload_total_bytes_total{CPU_to_GPU}`** | **> 0**（这是"能存取回"的直接证据） |
| ③ | **`external_prefix_cache_hits_total`** | **> 0** 且 replay 轮命中率 > 50% |
| ④ | **`replay TTFT ≪ fill TTFT`** | **≥ 3×**（A3 上 8 卡 14.8×、单卡 9.2×） |

**另加一条"池子账"**：`num_cpu_blocks ≥ 一轮待取回的条目数`
（`logs/013` §3.3：1.000× 全中、**0.977× 断崖归零**）⇒ 余量留 **≥1.2×**。

### 2.4 ★★ 长上下文的前提（**已按 `logs/017` 更正，这条改变了结论**）

`logs/017` 把"SWA 裁剪"这条路测透了，结论是**三句话**：

| # | 事实 | 强度 |
|---|---|---|
| 1 | **上游那条 `alignment_chunk_count` 钩子在 DSV4.1 上空转**（SWA 组 `tokens_per_chunk` 也是 1024，`_alignment_chunk_count()` 直接 `return None`）—— 它本是给"SWA block ≪ MLA block"的模型设计的 | 【实测】 |
| 2 | **"每请求每 SWA 组只留窗口 1 个 chunk"确实能把池子压到 1/3.14**（44→14 条，224 MiB 池四条判据全中、replay 47.5 vs fill 461.8 ms、**文本 sha256 16/16 相同**） | 【实测】 |
| 3 | ⛔ **但它对"变长前缀"不安全**：同池同代码，replay 换更短前缀（4096→2048）⇒ **`hits=0`、`CPU→GPU=0`、replay 239.8 ≈ 冷算 243.4 ms** | 【实测】 |

**⇒ 对真实会话（system prompt + 变长历史），"只存窗口"不能上线。**
安全下界下，池子需求**回到"SWA 全存"那一列**：

| 上下文 | 16 并发宿主实占 |
|---:|---:|
| 32K | **333 GiB** |
| 128K | **1,333 GiB** ⛔ **A2 不可能** |

### 2.5 ✅✅ 出路已经做出来了：**per-group `blocks_per_chunk`**（`logs/021`，实测）

**做法**：把 `blocks_per_chunk` 做成 **per-group**（SWA=1、full=8）、池记账单位改成"1 个 GPU block"
⇒ **上游那条本来就有、在 DSV4.1 上空转的 `is_store_reachable_swa_chunk()` 立刻生效**
（`alignment_chunk_count` 从 `None` 变成 **8**）。

**五条判据全过**（11 条臂）：

| # | 判据 | 结果 |
|---|---|---|
| 1 | 总字节 | **704 MiB → 144 MiB（4.89×）** |
| 2 | 四条判据 | ✅ `BlockStored:CPU=714`、`CPU→GPU=273 MB`、`hits=65,520`、**replay 49.9 vs fill 464.5 ms（9.3×）** |
| 3 | ★★ **变长前缀安全** | ✅ 4096 → **2048 回放**：`hits=32,752`、**replay 46.1 ms（10.1×）** —— 而同一代码下 `SWA_TRIM=window` 仍是 **0/0** ⇒ **§2.4 的反例被修复** |
| 4 | 文本 sha256 | ✅ 与 `017` 基线逐字节相同 |
| 5 | 单位 | 131,072 B 记账 / 910,208 B 物理（×6.945）⇒ **每请求 9 MiB（旧 44 MiB）** |

### 2.6 ★★★ A2 的最终容量账（**§2.4 的矩阵作废**）

| 上下文 | 口径 | 记账 ×16 | **宿主实占** | 判定 |
|---:|---|---:|---:|---|
| **32K** | SWA 全存 | 48.0 GiB | **333 GiB** | ⚠️ 占余量 75% |
| | **★ per-group bpc** | **9.5 GiB** | **66 GiB** | ✅ **宽裕** |
| **128K** | SWA 全存 | 192.0 GiB | **1,333 GiB** | ⛔ **不可能** |
| | **★ per-group bpc** | **38.0 GiB** | **264 GiB** | ✅ **可以**（占余量 60%） |

**⇒ ★ A2 的 128K × 16 并发从"不可能"变成"可以"。**

### 2.6b ★★ 并发 × 上下文的边界（**A2 到底能扛多少**）

| 场景 | **宿主实占** | A2（余量 442 GiB） |
|---|---:|---|
| 16 × 32K | **66 GiB** | ✅ 宽裕 |
| **16 × 128K** | **264 GiB** | ✅ **可以**（60%） |
| 32 × 128K | **528 GiB** | ⛔ 不可能 |
| **64 × 128K** | **1,056 GiB** | ⛔ **不可能** |

★ **但这是最坏口径**（每请求前缀互不相同）。真实 agent 流量是**共享 system prompt + 变长历史**，
此时池子只需覆盖**唯一前缀总量**：

| | 要覆盖的 | 1.000× 池 |
|---|---|---|
| 最坏（前缀互不相同） | Σ 各请求长度 | 16 × 128K ⇒ **264 GiB** |
| **典型（共享同一前缀）** | 唯一前缀总量 | ≈1 个前缀 ⇒ **16.5 GiB** |

**⇒ 差 16 倍。** 「64 并发」在最坏口径下不可行，但在共享前缀下**很可能完全可行**。
（`L3_8card` 正在测这条，结果会给出真实场景的池子需求。）

> ★★ **本表是「L5（per-group bpc）only」的口径，已被后两条杠杆实测改写** —— 见 §2.6c。
> ⇒ 下面的 `OFFLOAD_GB=48` 是"**现在就够用**"的值，不是"只能这么大"。

### 2.6c ★★★ 容量杠杆的**实测**账（2026-09-22 03:1x 更新）

| 杠杆 | 机制 | 倍数 | 状态 |
|---|---|---:|---|
| **L5** | per-group `blocks_per_chunk`（SWA 条目粒度） | **4.89×** | ✅ **实测**（`logs/021`，五判据全过含变长前缀安全） |
| **L1** | 池张量按"引用它的组真正需要的行数"分配（原来 16 张各 `num_cpu_blocks` 行） | **1.96×** | ✅ **实测**（`logs/030`，两条工作集上都是 1.96×；**不是 `029` 推测的 16×**） |
| **L2** | 让 `worker_kv_bytes_per_block` 记账诚实（原来低估 6.944×） | **6.944×（口径，不是容量）** | ✅ **实测**（`logs/032`：宿主乘数 **×6.944 → ×0.996**）—— 它的价值是**让 `OFFLOAD_GB` 这个旋钮说真话**，不是省内存 |
| ~~**L6**~~ | ~~8 份 worker 副本合成 1 份（`replicated_layout`）~~ | **暂不推进** | ⛔ **`logs/032` 的裁决**：上游**两道门都关着**（门 1 要求"单组 + 裸 `MLAAttentionSpec` + `worker_kv_bytes_per_block == page_size × layers`"，V4.1 是 13 组 + hybrid 页几何 ⇒ 结构性冲突；门 2 是 Ascend `_uses_shared_region()`）；**8× 的 V1 前提（8 rank 逐字节相同）端到端未证**（残余风险 = MoE/TP 集合通信的浮点归约顺序）。⇒ **从合并账里删掉**，容量账只认 **L5 + L1 + L2** |

**L1 实测推翻 `029` 推测的「16×」，原因**（`logs/030` §3）：16 张张量**不互相独立** ——
12 张只给 `full` 用，另 4 张被 **`state` + 10 个 SWA 组共同引用**；
而 SWA 条目的**有效载荷本来就几乎填满一行**（4×131,072 B vs 一行 540,928 B）⇒ **真实可省 ≈ 1.96×**。

**★ 叠加后的 A2 容量账**（`logs/030` §5；宿主实占，余量 442 GiB）：

| 场景 | L5 only（现状计划） | **+L1（实测 1.96×）** | +L1 & KV8+ring16（×1.913） | +L1 & SWA-only+ring16（×1.4655） |
|---|---:|---:|---:|---:|
| 16 × 32K | 66 GiB | **33.6 GiB** | 17.6 G | 22.9 G |
| **16 × 128K** | 264 GiB | **134 GiB** | 70 G | 91 G |
| **32 × 128K** | 528 GiB ⛔ | **269 GiB ✅ 从"不可能"变可行** | 141 G | 183 G |
| **64 × 128K** | 1,056 GiB ⛔ | 538 GiB ⛔ | **281 G ✅** | 367 G |

> ⚠️ **★ 上表的"每请求 unit 数"来自模型（≈19/1024token）；`027` 的 8 卡第一手反解是 ≈23.5/1024token（高 18%）**，
> 且它给出的是**"1.000× 池仍会因回放轮的 churn 级联归零"**（`A'` 臂 0.844× 归零、`B` 臂 0.852× 也归零）
> ⇒ **实际配池要在"实测需求"上再留 ≥1.2× 余量**，不能按 1.000× 配。
> 待 `logs/027` 的 `B2`（16×128K、池 = 48,064 unit 的实测 1.000×）落地后，**本表要按 23.5 与 1.2× 重算一遍**。

⇒ **`L5 + L1` 两条已实测的杠杆合起来 ≈ 9.6×**，把「32 并发 × 128K」从不可能变成可行，
**且不依赖任何未解的技术障碍**（KV8 的 prefill、L6 的 gate 都可后置）。

> ★ 表头那个 `×1.913 / ×1.4655` **不是"总倍数"，只是"页宽"那一维的乘数**；
> 与 L1（行数维）**相乘**才是总倍数：`+L1 & KV8+ring16` ⇒ **1.96 × 1.9133 ≈ 3.75×**
> （相对 L5-only 基线）。上面四列的**绝对值**已经把乘法算进去了。
> ⇒ **KV8 相对"只量化 SWA"的增量，只有 3.75/2.87 ≈ 1.31×**；
> 而 KV8 要付 prefill（未解）+ cmp 重建（+2.15%）的代价 ⇒ **优先做 SWA-only 那一档**（§3.4）。

### 2.7 上线顺序（**已更新**）

1. **短上下文（32K）现在就能上**：per-group bpc + `OFFLOAD_GB=16`（宿主 ≈111 GiB）；
2. **长上下文（128K）需要** per-group bpc + `OFFLOAD_GB=48`（宿主 ≈333 GiB，占余量 60%）；
3. **KV8 不再是 128K 的必需项**（per-group bpc 已经够了）—— KV8 的 1.135× 只是锦上添花，
   而且它现在还有 **+21% 时延**的硬伤（见 §3）。

---

## 3. 线 2：KV8 上线（**★ 2026-09-22 03:2x 大幅转正，见 §3.4**）

### ★★ 3.4 【实测】KV8 的两条硬伤**都已解**（`logs/033`）

| 项 | 旧状态（`logs/025`/`028`） | **新实测（`logs/033`）** |
|---|---|---|
| **prefill** | ⛔ 45–105 ms/step（`015`），`018` 的页粒度重建**没量过** | ✅ **+0.58 ms/step（host-bound 最坏）/ +0.00–0.06 ms/step（设备饱和，生产形状）** ⇒ 相对 015 **降 75–175×**；判据 ≤+0.5%/step ✅ |
| **容量** | ×1.913（解析） | ✅ **×1.9133（真起服 `GPU KV cache size` 实测）**，五档与解析**逐格吻合（<0.05%）** |
| **decode** | +2.15%（`028`） | +2.15%（不变），且 KV8 全量相对"只量化 SWA"**只多 +178 µs/step（+0.59%）** |
| **★ 正确性 bug** | — | ⛔ **新发现**：`dsa_v41.kv8_scratch_plane` 的缓冲键只有 `(blocks, block_size, dim)` ⇒ **窗口重建与压缩重建在页数相同时共用同一块 scratch**，后者覆盖前者（512-token chunk 下算子输出漂移 **1.13**）⇒ **上生产前必须按 `role` 分键**（`033` 的 kernel 已分键） |

**⇒ 结论改写（当时）**：KV8 从「可选增强」升为 **推荐项** ——
`L5 + L1 + KV8 + ring16` ⇒ **1.96 × 1.9133 ≈ 3.75×（相对 L5-only）**，
代价 **decode +2.15% + prefill ≤+0.015%（生产形状）**。

> ⛔⛔ **2026-09-22 07:1x 重大更正（`logs/035`→`036`→`038`→**`040`**）**：
> **int8 两条杠杆（SWA-quant / KV8）维持否决**，但**否决理由已第五次改写**，且**两个现象被彻底分开**。
> 四轮定位的结论链（每轮都推翻上一轮的表述，但证据越来越硬）：
>
> | 轮 | 结论 | 状态 |
> |---|---|---|
> | `035` | "int8 平面经**池往返不保真**" | ⛔ **被 `036` 证伪** |
> | `036` | "池往返**逐字节保真**"（12,928 次全字节比较 mismatch=0）；真凶 = "int8 SWA 面 + 对齐 prompt ⇒ 1 行 decode" | ⛔ **修点被 `038` 证伪** |
> | `038` | 真凶 = **"池 unit 数 == 工作集"的临界容量** | ⛔ **被 `040` 证伪** |
> | **`040`** | ★ **两个独立现象**：(A) `038` 那个只对 **`XL1=0`（L5 单位池）** 成立、**`L1` 一开即消失**；(B) int8 两条杠杆的 ❌ **与池容量无关**（池只用 **54–60%**），机制 = **卸载层拿 `page_size_bytes`(65,536) 当 DMA 拷贝长度、而行步长是 66,560/73,856** | ★ **当前口径** |
>
> **`040` 的两条关键实测**：
> ```
> ① 三臂对照（池大小/unit 数/淘汰数/CPU→GPU 字节【逐字相同】，唯一变量 = manager）：
>      int8 + PerGroupBPCManager(XL1=0) ⇒ ❌ 1/16
>      int8 + P2QuotaManager(XL1=1)     ⇒ ✅ 0/16  ← ★ 而本站推荐的 L5+L1 用的正是这个
>      BF16 + PerGroupBPCManager        ⇒ ✅ 0/16
> ② 四个几何的「state 行步长 vs 卸载拷贝」：
>      C0 int8/BF16  : 131,072 vs 131,072  ⇒ 差 0        ⇒ ✅
>      D 几何（4 条）:  73,856 vs  65,536  ⇒ 差 −8,320/行 ⇒ ❌
>      F 几何（5 条）:  66,560 vs  65,536  ⇒ 差 −1,024/行 ⇒ ❌   ← ★ 66,560 正是 034 预言过的数字
> ```
> ⇒ **运维要求：`OFFLOAD_GB` 仍必须留 ≥1.2× 余量**（`027` 给的 `56` = 1.193× 在边界之上，不要低于它）。
> ⇒ **已交付的防护**：`agents/G_kv8fix/kv8_nan_guard.py`（写侧 fail-fast）+ `agents/I_unitprobe/` 的行级 provenance 自检（`I_UNITPROBE_MODE=error`）。
> ⇒ **正在做**：`L_dmafix` 修那条 DMA 拷贝长度（**若成功 ⇒ int8 两条杠杆解锁**）。

### ★★ 3.5 【实测】`logs/034` 对 §3.4 的三处修正（**以本节为准**）

| # | 修正 | 依据 |
|---|---|---|
| **1** | **ring 缩 FP16 单独用 = ×1.0000（白做）** —— 引擎臂 `A3`（无 KV8 / ring F32）与 `A2`（无 KV8 / ring **FP16**）**都是 `22,719`**。机制：ratio-2 槽 = `max(kv+index 73856, ring 131072, **BF16 SWA 别名 131072×10**)` —— ring 与 SWA 别名**同值并列顶着**，ring 减半后 **SWA 页立刻接管** | 【实测】两条独立路径（`033` + `034`）**逐字相同** |
| **2** | **ring FP16 与 ring INT8 容量完全相同（都 ×1.9122）⇒ INT8 那套 scale 不用做**（省一整条支线） | 【实测】同上 |
| **3** | ★ **033 §5.4 的一处推断被纠正**：「只量化 SWA + **F32** ring」= **×1.0000**（**不是** ×1.135）；「只量化 SWA + **ring16**」才是 **×1.4655** | 【实测】六条臂 100% 到 `/health`（含 `033` 死掉的那三条） |

**★ 数值安全性（这是 `028` 缺的那一格）**【实测】：真权重 `wkv/wgate` + 真 kernel + **CPU fp64 golden**，层 2/8/14 × L=1K/8K/32K × B=8 ——
```
地板（bf16 输出本身）      = 1.6565e-3 ~ 1.6583e-3
F32 ring 总误差            == 地板            ← 现状本来就坐在地板上
FP16 ring                  = 地板 × (1+0.41%~0.51%)
L 涨 32× 只漂 ≤+0.7%，首/中/尾三段差 <1%  ⇒ ★ 无累积
```
⇒ **ring FP16 的数值代价 = +0.4~0.5%（相对地板），且不随上下文长度累积** —— 这是它敢上线的根据。

**★ 补丁规模与"免费"的证据**：**6 个逻辑点 / 21 组替换 / 29 处落点**（含 3 个 kernel 签名）。
`028`/`033` 都漏了「`reshape_cache` 用**槽页步长**建 state 视图 ⇒ ring 减半后张量不连续」⇒ **必须把页步长传给 kernel**；
该改动在 FP32 下**逐比特 no-op**（原版 vs 影子 digest 同为 `-477976451`）。判据 ④：**图内 Δ∈[−0.11,+0.26] µs/步 = 噪声 ⇒ cast 免费**。
**只 SWA+ring16 的时延 = +10.61 µs/层 × 40 = +424 µs（+1.34%）**。
★ **记账区间（`034` §8 按"不许用相邻数字顶替"的规矩放宽）**：
同 harness 三组独立读数 = **+1.34% / +1.55% / +1.75%** ⇒ **不含 KV8 记 `+1.3%~+1.8%`（中位 +1.55%）；含 KV8 记 `+1.8%~+2.2%`（中位 +2.15%）**。
（`+1.75%` 是 `028` 自测、`033` 又独立复现过的那一格，不能排除在区间外。）

**★ 依赖顺序（重要）**：`ring16` 单独上 = ×1.000（白做）⇒ **必须先有 ring16，SWA-quant/KV8 才拿得到容量**。
反过来不成立（SWA-quant 单独上、ring 仍 F32 = ×1.000）。

### 3.1 现状

| 阶段 | 状态 |
|---|---|
| 精度前提 | ✅ `logs/002`：INT8 g128 → rel_L2 2.21%、cos_min 0.990、**无尾部** |
| **TND 路线** | ⛔ **否决**（`logs/015`：arch22 只编译 `TND Q × PA_BBND KV`） |
| **替代设计** | ✅ **PA_BBND scratch + identity block table + 索引重编号**，逐比特精确 |
| **性能（图内）** | ✅ **+0.23%**（+0.068 ms/step） |
| **性能（chunked prefill）** | ❌ **45–105 ms/step 否决** |
| **引擎集成** | ✅ **已完成**（`logs/018`）：无损臂 `max_abs=0 / rel_L2=0`；真量化 `rel_L2=5.42e-3 / cos=0.999986`；**图内 +0.07%** |
| **⛔ 容量收益** | **只量化 long-KV：×1.032**（`logs/018`）；**连 SWA 一起量化：×1.135**（`logs/020`）—— **都不是 ×1.84/×1.98**，见 §3.2 |
| **✅ 图内性能（融 kernel 后）** | **+45.57 µs/层 ⇒ +0.684 ms/step（+2.15%）**（`logs/028`；原 +358 µs/层/+21% 已由两个 Triton-Ascend kernel 压掉 87%） |
| **✅ 容量（state ring FP16 后）** | **×1.912**（`pool_bytes_per_block` 476416 → 282880；`logs/028`） |

### ★★ 3.1b 容量收益为什么拿不到（**两层原因，第二层是 `logs/020` 才发现的**）

**第一层（`logs/018`）**：hybrid slot 把 long-KV 平面与 10 个 SWA 平面**叠在同一物理页几何**，
页大小取 `max(long-KV 页 + indexer 页, *alias 页)`；**BF16 SWA 页 = 128 × 512 × 2 = 131072 B**
永远大于 INT8 long-KV 页 ⇒ 只量化 long-KV 只到 **×1.032**。

**第二层（`logs/020`，★ 这一层才是真凶）**：把 SWA 也量化（`scale_dim` 加上、SWA 页
**131072 → 66560 B**）之后，容量**只到 ×1.135（540928 → 476416 B/block）**，不是 ×1.98。
原因：**4 个 hybrid slot 里有 3 个（ratio-2 槽）的页被 FP32 compressor state ring 顶住**：

```
DeepseekV41CompressorStateSpec = 32 行 × 1024 dim × 4 B(F32) = 131072 B/block
                                              ↑ 逐字等于被 SWA 顶住的那个数
```

⇒ **缩了 SWA 也一分钱不省**（020 §2 有分槽逐项对账）。

### ★★ 3.1c 两条 P0（需要决策）

| # | 问题 | 结论 / 建议 |
|---|---|---|
| **P0-1** | **图内性能不达标**：生产形状整图 **+169 µs/层（SWA）/ +267 µs/层（cmp）** ⇒ **≈+7.8 ms/step（+26%）**；而**预置 scratch 后算子只 +0.16 µs/层** ⇒ **成本 100% 在 gather/dequant** | ★ **先修这个** —— `logs/015` 已标定瓶颈是 `kv_i8[phys]` **二维高级索引（~20 GB/s）**，而 **flat `index_select` 达 1.2 TB/s（60×）** ⇒ 换快路径后有望降到 <1% |
| **P0-2** | **容量分母是 compressor state ring**（F32 32×1024） | 缩它超出 KV8 范围（要动 `AscendCircularBufferSpec`）；缩了之后是 **×1.91**【推断】 |

**⇒ 决策：先做 P0-1（性能），因为性能不达标时容量再大也是负收益。**
已派子代理 **KV8_gather** 去做（见 `logs/023`）。

### 3.2 上线的前提（★ 已按 `logs/018` 更正）

| # | 前提 | 状态 |
|---|---|---|
| 1 | 数值对拍（`rel_L2 ≤ 2.3%`） | ✅ **已过**（5.42e-3）；**但 prefill 形状有 NaN**【未确认】 |
| 2 | **容量验证**（3.50M → ~6.4M） | ⛔ **未过**（只有 ×1.032）⇒ **必须先做「SWA 也量化」** |
| 3 | **prefill 侧必须回退 BF16**（或找别的解法） | ⏳ `018` 已做"页粒度整前缀重建"绕开 015 的行放大，但 NaN 未定性 |
| 4 | 与卸载**叠加验证** | ⏳ 未做 |

### 3.2b ★★★ KV8 的裁决已修正（**`logs/028`，2026-09-22 02:3x**）

| 项 | 旧结论（`logs/025`） | **新实测（`logs/028`）** |
|---|---|---|
| 时延 | +21% | **+2.15%**（融 kernel：整层 +357.86 → **+45.57 µs/层**） |
| 容量 | ×1.135 | **×1.912**（state ring 缩 **FP16**；`pool_bytes_per_block` 476416 → **282880**） |
| 精度 | 担心 ring 是累积状态 | ★ **白送**：ring 存的是 `[kv|score]` 的**原始 FP32 投影**，而池化输出**本身就是 BF16** ⇒ FP16 下 `rel_L2=7.6e-4` **低于**现有 bf16 地板 1.66e-3 |

**⇒ 权衡 = 「+2.15% 时延 ↔ +91% 容量」⇒ 是赚的。KV8 从"降级为未来候选"改为「可选增强」。**

★ 而且 **HBM 用的是同一个页几何** ⇒ **HBM KV 也涨 ×1.912**（3.50M → **~6.7M token**）：

| | 不上 KV8 | **上 KV8** |
|---|---:|---:|
| HBM KV cache | 3.50M token | **~6.7M token** |
| 128K × 16 并发的 DRAM 池 | 264 GiB | **138 GiB** |
| 时延 | 基线 | **+2.15%** |

> ⚠️ **为什么做不到 ≈0% —— 这是被证明的，不是"没调好"**（`logs/026` §6.1，两条独立论证）：
>
> | 论证 | 数 |
> |---|---|
> | 融合后每步**仍必须搬**的字节 | **151.8 MB**（40 层 SWA × 3.16 MB + 4 层 cmp × 6.34 MB） |
> | 本 die 连续拷贝上限（实测标定） | **1161 GB/s** |
> | ⇒ **理想地板** | **131 µs/步 = 0.44%**（判据 0.2% 的 **2.2 倍**） |
> | 要进 60 µs 需要 | **≥2530 GB/s** ⇒ **不可能** |
> | 实测融合版 | 151.8 MB / 0.63 ms ≈ **241 GB/s**（地板的 21%） |
>
> **⇒ 判据 2（+0.2%）在"重建"这个形态下物理不可达。**
> 要真正 ≈0% 只有两条路：**① 让 attention 算子直读 int8**（需 CANN 侧支持，本镜像的
> `kv_quant_*` 有 576 的硬约束，见 `logs/007`）；**② 把需要重建的层数从 44 降到 ≤4**。

### 3.3 预期收益（**已按 ¶3.4 更正**）

```
【原预期】4421 -> 2405 B/token（x1.84）
【实测】  4226 -> 4096 B/token（x1.032）   ← long-KV 单独量化拿不到
【实测】  连 SWA 一起量化 => 3722 B/token（x1.1357）
【实测】  + ring16        => 2885 B/token（x1.4655）   ← ★ 不碰 long-KV 的中间档
【实测】  + 双平面 KV8    => 2210 B/token（x1.9133）   ← ★ 判据 ×1.84 达标
```

> ⛔ **本节已被 §3.4 末尾的更正推翻**：prefill + 容量 + 精度三项确实都过，
> 但 **`036`/`038` 发现了一个第四项硬前置** —— **池容量必须是工作集的 ≥1.2×，否则 int8 几何下会静默 NaN**。
> ⇒ **线 2 维持否决**（不是"可以上线"）；修点在**卸载层的 unit 记账/分配**（`038` §11 给了下一步一条命令）。
> **线 1（`L5 + L1`）的结论不受影响**（它是唯一"容量 ×1.96 且逐字节一致"的组合）。

---

## 4. 交付物落点

| 类 | 位置 |
|---|---|
| 上线脚本 | **验证通过后**进 `../dsv41-release/scripts/`（发布包只收已验证的） |
| 补丁 | `a2/agents/{D2_offload,P1_pinned}/patches/` → 验证后进 `../dsv41-release/patches/` |
| 参数与判据 | 本文 + `logs/019` |
| 实验证据 | `a2/logs/NNN-*` + `a2/logs/raw/NNN-*` |
| **对外预览分支** | `chiro2001/deepseek-v4.1-flash-ascend910B` → **`feat/kv8-dram-offload-pending`**（线 1 已验证、线 2 待测；未提 PR / 未提 issue） |

---

## 5. 未确认 / 风险

| # | 事项 | 影响 |
|---|---|---|
| 1 | **A2 的两条探测还没跑** | 决定候选 β 是否可用 ⇒ **阻塞线 1 上线** |
| 1b | ★★ **A2 上必须复跑 `a2_pinned_probe.sh`**（A3 全绿不代表 A2 全绿） | 同上 |
| 2 | ~~SWA 裁剪未实现~~ → **已由 per-group `blocks_per_chunk` 解决**（§2.5，`logs/021` 五判据全过） | 已闭环；**`SWA_TRIM=window` 那条近似规则已弃用**（`logs/017` 证明它对变长前缀整轮归零） |
| 3 | **6.945× 记账系数是 A3 的数**（`logs/016` 的 58 GiB→403 GiB 与 `logs/021` 的 131,072→910,208 两处独立吻合） | 数值本身跨机型成立（是分配结构决定的），但**A2 上仍要按实占复核一次** |
| 4 | ~~KV8 的 prefill 否决~~ → **`033` 已解**（+45~105 ms → +0.00~0.06 ms） | 已闭环 |
| 4b | ★★ **int8 几何下 state 行步长 ≠ 卸载拷贝长度**（`040`：66,560 vs 65,536 ⇒ **每行尾部 1,024 B 从未被 store**） | **线 2 的硬约束**；修点是**一行级**（DMA 拷贝长度改用真实行步长）；`L_dmafix` 在做 |
| 5 | `state` 组跳过的**数值正确性**只做了语义推断 | `logs/009` §6 已标【未确认】，建议补精度对比 |
| 6 | ~~L1 / L6 未实测~~ → **L1 已实测 1.96×**（`030`）；**L6 已判"暂不推进"**（`032`） | 已闭环 |
| 7 | ★★★ **`temperature=0` 下服务本身不确定**（`037`：32K 2/4、512 短 prompt 3/4 首 token 不同） | **与 KV 容量无关的独立质量问题**；用户报的"服务质量下降"里有一部分来自它 ⇒ **需要给 A2 一个"确定性配置"**（`037` 只做到"关投机后重复请求 4/4 稳定"） |
| 8 | **`state` 组被排除卸载的副作用** | `040` 已把机制改判到"DMA 拷贝长度"（见 4b）⇒ 本条**降级**，但仍未排除 |
| 9 | ~~`kv_bytecheck.py` 没跑成~~ → **`039` 已跑**（tiny 干净：13,392 区间 × 2 格 mismatch=0、账本字节逐位等于引擎 metric）；★ **但该脚本本身不可用**（hook 名不存在却打"已装"⇒ 会得到"全绿但比对 0 次"），已点名 | 线 1 的 **8 卡保真仍【未确认】**（`039` 的遗留） |
| 10 | ★ **`039` 发现的 store↔load 口径不一致**（生产代码自己构造的 spec：group 0 是 store 1 块/chunk vs load 8 单元/chunk，448/512 读全 0 行） | **可能落在可上线路径上**；`H_kvcheck` 拒绝在读数冲突时下结论，定性格待做 |
| 11 | ★ **L1 只有 tiny 实测**（`027` 的 8 卡臂**不含 L1**） | 决定 A2 的内存占用是 389 GiB（88%）还是 198 GiB（45%）⇒ `K_l1_8card` 在做 |
