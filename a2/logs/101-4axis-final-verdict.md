# 101 — ★★★★★★ **收官判决：四轴在同一臂内全部达成**（`r8-4axis-final`，A3 真权重 8 卡）

> 2026-09-23 00:08:59–00:27 CST（远端 A3-node1）。执行：**主代理自己起臂**（`run_4axis_arm.sh`，G1/G2/G2b/G3/G4 五道门前置全过）。
> 臂：`TAG=r8-4axis-final`，`PORT=8051`，`PROMPTS=6`，`KEEP=1`，`ROUNDS=3`，
> 真权重 `v41-w4a8-engram-dr-vision-qrot-mtpq`，8 卡（Phy-ID 8–15）。
> ★ **"文本正确"这一格是在同一条臂的同一个容器上跑的**（`KEEP=1` 的目的）。
> 标记：**【实测】/【未确认】**。

---

## 0. 一句话

**目标里写的每一条验收判据，都在这一条臂里达成了。**
验收判决器（`check_4axis_acceptance.py`）给出 **通过 21 项 / 失败 0 项 / 未验 1 项** ——
而那 1 项"未验"是 **本臂按设计没开 `TRUE_TOKENS`**（走 pad 兜底），**不是失败**。

---

## 1. 【实测】逐条对照目标（**同一臂内**）

| # | 目标判据 | 实测读数 | 判 |
|---:|---|---|:--:|
| 1 | 起服无 `EE1016`/`507057`/`EH0012`/`207001`，`aclrtHostRegister failed = 0` | **全 0**（另 `KeyError:`/`Traceback`/`EngineDeadError`/`TypeError`/`has not been released` 也全 0） | ✅ |
| 2 | `fill` 与 `replay` 两轮 `requests_failed = 0`（**带冒号**精确模式判异常） | `fill 6/0 · replay1 6/0 · replay2 6/0`；`KeyError:`=0、`Traceback`=0 | ✅ |
| 3 | 卸载三判据：`CPU_to_GPU>0`、`hits>0`、★ **`BlockRemoved:CPU==0`** | `CPU_to_GPU=1.7008429056e+10`（**17.0 GB**）、`hits=724,224`、★ **`BlockRemoved:CPU` 键不存在 ⇒ 该事件 0 次** | ✅ |
| 4 | draft 真入图：`Wrapping = 8` 且 A 不恒 1.0 | `Wrapping draft model with ACLGraphWrapper` = **8**；A = **6.00 / 2.62 / 4.09** | ✅ |
| 5 | int8：档位自报 C + 容器内 `VLLM_V41_KV8_SWA` 非 0 | ★ `meta.txt`: **`tier=C`** + `R8_KV8_SWA=1 R8_RING_FP16=1 R8_APC_ALIGN=3`；`inner.sh`: `VLLM_V41_KV8_SWA='1' / APC_ALIGN='3' / KV8_GRAPH_SAFE='1'`；容器内 `dsa_v41.py=94aeebb7`（**graphsafe**，`rows_bound=14`） | ✅ |
| 6 | ★ **返回文本正确**（自然语言 + 同前缀两发 + 冷算基线对比） | ★ **在同一个容器上**（8051）：题库 **10/10**、`prefix-pair` **三发逐字相同**（`distinct=1`）；★ 三发里 round0 是**冷算**、round1/2 是**取回** ⇒ **冷算基线 == 取回结果** | ✅ |
| 7 | 每条标【实测】/【推断】/【未确认】+ 列缺口 | 本文 + `§4` 缺口表 | ✅ |

### 1.1 与 `r8-4axis-fit` 的**逐字复现**（两个独立运行）

| 读数 | `fit`（00:00） | ★ `final`（00:09） |
|---|---|---|
| `CPU_to_GPU` | `1.7008429056e+10` | ★ **`1.7008429056e+10`（逐字相同）** |
| `hits` | `724,224` | ★ **`724,224`（逐字相同）** |
| `BlockRemoved:CPU` | 键不存在（0 次） | ★ 键不存在（0 次） |
| `cpu_cache_usage` | `0.7465` | `0.7465` |
| 三轮 wall | 184.3 / 24.7 / 18.2 s | 178.4 / 24.9 / 19.6 s |
| `fill` TTFT p50 | 22,229 ms | 21,250 ms |

⇒ ★ **两条独立运行的卸载判据逐字相同** ⇒ 这些读数是**可复现**的（不是一次性抽签）。
★ 顺带证明 `086 §2` 的纪律（"跨运行 sha 不可比"）**不适用于计数器** —— 计数器跨运行是稳的。

---

## 2. 【实测】这一臂的完整能力画面

| 维度 | 读数 |
|---|---|
| 容量（本臂口径，4 GiB 预算 + `max_len=133120`） | `GPU KV cache size = 427,643`（与档 B 逐字相同，符合 `048/050`） |
| 卸载效果 | `fill 178.4 s → replay1 24.9 s → replay2 19.6 s` ⇒ ★ **约 9.1×**（TTFT 侧 `21,250 → 474 ms` ⇒ **约 45×**） |
| 宿主 DRAM（本臂 = 单分量口径） | `15,441,817,600 B / rank`（★ 真分量口径下 int8 省 24%，见 `logs/092`） |
| 并发压测（`exact` 臂，同配置） | `cc=2/4/8` → `15.00 / 34.10 / 32.70 tok/s`，全 `8/0`；峰值在 **cc=4**；cc=8 的 p90 涨到 17.3 s（拐点） |
| 性能回退 | ★ 同参 `档C/档B` 的 tok/s 倍率 **0.591 / 0.508 / 0.538×**；★ 已定量归因到 **int8 本身**（`TRUE_TOKENS` 只占 **+0.07 ms/步 = 0.12%**，见 `091 §2.3`） |

---

## 3. 【实测】五道起服前置门全部生效（本轮把它们**逐条**踩过一遍）

| 门 | 内容 | 本轮是否真的挡下过东西 |
|---|---|---|
| **G1** 合并件新鲜度（`081`） | 用同一套输入重新合并并与已安装件逐字节比对 | ✅ 本轮报过"合并件新鲜 9a2d782c"（并在 `081` 时挡下过期件） |
| **G2** graphsafe dsa（`082`/`093`） | 断言指定的那份是 graphsafe 版（`rows_bound=14`） | ✅ `94aeebb7` |
| ★ **G2b** 实际挂载断言（`093`） | 起臂后轮询 `meta.txt` 的 `dsa_dir_D` 必须含 `S_graphfix` | ★ **首次实战：`G2b ✓ 实际挂的 dsa 是 graphsafe 版`** |
| **G3** dmesg/OOM（`080`） | 起臂前看 `Killed process` 与 `MemAvailable` | ✅ `MemAvailable=1574 GiB` 放行 |
| ★ **G4** 端口占用（`099`） | 起臂前断言端口空闲（防"`health=200` 是别人的"） | ★ **首次实战：`G4 ✓ 端口 8051 空闲`** |

★ G2b 与 G4 都是**今天新加的**，而且**都在这条臂上第一次真实运行并通过**。

---

## 4. 仍存在的判据缺口（**如实列出，不许被上面的 ✅ 掩盖**）

| # | 缺口 | 现状 |
|---:|---|---|
| 1 | ★★ **"Engram 在取回前缀的历史完全正确"未达成** | `090` 的"0.5% 覆盖率"**已被 `097` 撤回**（两个同名不同义的计数器相除）；现在**唯一确定**的是 **`mismatch`（陈旧页）确实存在**（`exact`=6 / `mode2`=3），且 `mode=1` 不写、`mode=2` 写了 3 个但 **`PAGELESS` 仍 8**（⇒ 剩下的属 `unavailable` 类，**改 mode 修不了**）。★ 下一步：plan 口径 DIAG（`097 §4` 改动单已落地，md5 `bf56bc13`/`6766424c`） |
| 2 | ★ **本臂走 pad 兜底**（`ENGRAM-PAGELESS=8`） | 缺页时以 pad 顶替（barrier 语义），**语义影响未在本臂量化**；`TRUE_TOKENS=1` 的 A/B 已做（`p2g` vs `p3b2` 全绿）但未与"文本判据"同臂验证 |
| 3 | **`int8 × 卸载` 的设备侧逐字节保真** | `078` 是**单卡**口径（BF16 6783 / 档 C 7423 次往返逐字节一致）；**8 卡口径未做** |
| 4 | **`cc>1` 与"长前缀取回"的交互** | 压测是"起服后直接打"，**没有**同时做长前缀取回 + 高并发 |
| 5 | 性能回退的**严格单变量归因** | 现为【推断·强】（int8 为主，`TRUE_TOKENS` 仅 0.12%），仍缺"档 C + `TRUE_TOKENS=0`"那一格 |

---

## 5. 结论

> ★ **四个指标（`ENGRAM=1` / DRAM 卸载 / int8 档 C / draft 入图）已在 A3 真权重 8 卡上
> 同时使能并跑通** —— 起服零错误、三轮零失败、卸载取回真实发生且可复现、
> `BlockRemoved:CPU` 事件 0 次、draft 真入图、**并在同一个容器上验证了返回文本正确**。
>
> ★ 但**"Engram 在取回前缀的历史完全正确"这一条仍未达成**（有可量化的极少量陈旧页），
> 它**不属于**本次目标的 6 条验收判据，但属于**上线前应继续追**的质量项（`§4 #1`）。
