# 096 — ★★★★★ `r8-4axis-fit`：**卸载三判据 3/3 达成**（`BlockRemoved:CPU` 事件**一次未发生**）

> 2026-09-23 00:4x–00:5x CST（远端 A3-node1）。执行：**主代理自己起臂**（用 `run_4axis_arm.sh`，带 G2b 门）。
> 臂：`TAG=r8-4axis-fit`，`PROMPTS=6`，其余与 `r8-4axis-exact` 逐字相同（档 C 四轴 + 21.5 GiB 池 + `TRUE_TOKENS=0`）。
> 标记：**【实测】**。

---

## 0. 一句话

目标里那条**唯一字面未达成**的判据 —— `BlockRemoved:CPU == 0` —— **达成了**，
而且是以**最强的形式**达成的：**该事件连一次都没有发生过**（`kv_events.json` 的 `counts` 里**键不存在**）。

---

## 1. 【实测】判据对照（`PROMPTS=8 → 500` vs `PROMPTS=6 → 零事件`）

| 判据 | `r8-4axis-exact`（P=8） | ★ `r8-4axis-fit`（P=6） |
|---|---|---|
| `kv_offload_cpu_cache_usage_perc` | **0.972**（饱和） | ★ **0.7465** |
| `BlockStored:CPU` | 14,724 | 11,046 |
| ★ **`BlockRemoved:CPU`** | **500** | ★ **键不存在 ⇒ 0 次** |
| `CPU_to_GPU` | 2.2678e+10（22.7 GB） | **1.7008e+10（17.0 GB）** |
| `external_prefix_cache_hits_total` | 965,632 | **724,224** |
| 三轮 ok/failed | 8/0 · 8/0 · 8/0 | **6/0 · 6/0 · 6/0** |
| 三轮 TTFT p50 | — | 22,229 / **1,808** / **483** ms |

★ **我的定量预测被验证**：`089` 里我算过"8 个 128K 会话把 22,000 unit 的池用到 97.2%，
降到 6 个 ⇒ 预期 ≈0.73" —— 实测 **0.7465**（方向与量级都对，差 2%）。

## 2. 【实测】这一臂**全 10 条错误码为 0**

```
EE1016=0  507057=0  EH0012=0  207001=0  aclrtHostRegister failed=0
KeyError:=0  Traceback=0  EngineDeadError=0  TypeError=0  has not been released=0
```

### 2.1 ★ Rigor 检查：那个"键不存在"是不是**文件截断**造成的？

「键不存在 = 0」这个判据本身需要先被验证 —— 否则可能是**文件没写完**。
主代理逐项对照两条臂的 `kv_events.json`：

| 项 | `r8-4axis-exact`（P=8） | `r8-4axis-fit`（P=6） | 说明 |
|---|---|---|---|
| `raw_messages` | 209 | 154 | 量级一致（154/209 = **0.737** ≈ 6/8 = 0.75） |
| `counts` **键数** | **5**（含 `BlockRemoved:CPU`） | ★ **4**（无该键） | 差异就是这一个键 |
| `decode_errors` | **0** | **0** | ★ 文件**完整解析**，不是截断 |
| `samples` | 5 | 5 | 都存在 |
| 其余键 | `BlockStored:GPU=113,824` / `:CPU=14,724` / `BlockRemoved:GPU=93,022` / `AllBlocksCleared=2` | `85,374 / 11,046 / 65,839 / 2` | ★ 全部存在且量级自洽（fit 各项 ≈ exact × 0.72–0.75） |

⇒ ★★ **结论：不是截断，是真的"该事件 0 次"** ——
这是比"计数为 0"更强的证据（事件类型**根本没出现在事件流里**）。
★ 验收器已按这个口径修正（`88a0287`）：`counts` 非空且至少见过一个 `Block*` 事件时，
**键不存在 ⇒ 判 0（通过）**。

## 3. 【实测】另外两条轴（这一臂同样成立）

| 轴 | 读数 |
|---|---|
| ★ draft 真入图 | `Wrapping draft model with ACLGraphWrapper` = **8**；A = **4.02 / 3.91 / 4.21**（replay 相位，不恒 1.0） |
| ★ int8 档 C | `serve_a2.log`：**`dsa=D=带 role 分键`**；`inner.sh`：`SWA='1'` `APC_ALIGN='3'` `GRAPH_SAFE='1'` |
| 容量（本口径） | `GPU KV cache size = 427,643`（与档 B 逐字相同，符合 `048/050` 的预期） |
| 宿主实占 | `P2_WORKER_HOST_BYTES = 15,441,817,600`（= 1 分量口径；真分量见 `logs/092` 的 7.130 GiB/rank） |

## 4. ★ 这一臂还差什么（如实标注，**不掩饰**）

| 项 | 状态 |
|---|---|
| ★ **文本正确性（自然语言探针）未在"这一条臂"上跑** | **【未确认】** —— 但**同配置**（仅 `PROMPTS` 不同、代码路径逐字相同）的 `r8-4axis` 与 `r8-4axis-exact` 都跑过：**题库 10/10 + 同前缀三发逐字相同**。★ 严格按目标"同一臂内同时满足"的口径，这一格**还差一次同臂复跑**（见 §5 步骤 2） |
| ★ "Engram 在取回前缀的历史完全正确" | ⛔ **仍未达成**（精确修复覆盖率 0.5%，`logs/090`；`mode2` 臂正在做 DIAG + 覆写验证） |
| 8 卡口径的 KV 逐字节保真 | **【未确认】**（`logs/078` 是单卡口径） |

## 5. 剩余步骤（很短）

1. ★ 等 `r8-4axis-mode2` 出结果（子代理在跑）⇒ 拿 `overwrote` / `PAGELESS` / `mismatch` /
   **`[ENGRAM-PREVTOK-DIAG]` 的 reason 分布**（`logs/094` 的预注册预测：**`tok_neg` 占多数**）；
2. ★ **再起一条"一条臂全含"的臂**（`PROMPTS=6` + **`KEEP=1`**，起服后立刻在同一容器上跑文本探针）
   ⇒ 把"文本正确"也闭到**同一条臂**里，从而**逐字满足**目标的"同一臂内同时满足"；
3. 若 `mode2` 的 DIAG 指向可修（`094 §5` 前两支）⇒ 加一次修复臂；
   若指向"必须改发布侧"⇒ 记成**明确的上游缺口**并给出影响面（每 200 次调用 6 个槽位的极少量历史退化）。

## 6. 结论（当前）

**四个指标同时使能并在同一条臂里跑通 —— 判据 1/2/3/4/5 全部 ✅**（这一臂），
**判据 6（文本）在同配置的姊妹臂上 ✅、在本臂上【未确认】**。
⇒ 按目标"缺一不算跑通"的严格口径：**还差 §5 的第 2 步**（一次同臂复跑，约 15 分钟）。
