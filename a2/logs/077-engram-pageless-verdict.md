# 077 — ★★★★ `ENGRAM=1` × 卸载 的 P0 修复 **A3 端到端判决：成立**（引擎不再死，replay 16/16）

> 2026-09-22 20:53–21:07 CST（远端）。执行：子代理 **`engram_repro_fix_arm`**（8 卡 c0 / 真权重），
> 主代理独立复核客户端产物与计数器。臂：`p2g-fix075-engram1-dev0-dg1`。
> 前置：`075`（修复本体）/`073`（判决）/`072` §3.3（anchor）。标记：**【实测】/【推断】/【未确认】**。

---

## 0. 一句话

**修好了。** 与 `p2e` 同形态的那条臂（`ENGRAM=1` + `ENGRAM_DEVICE_INDEX=0` + 卸载 + `DRAFT_GRAPH=1`）
这次 **fill 16/16 + replay 16/16 全过**、引擎全程存活；
**降级路径真的被走到**（8 rank 各打一次 `[ENGRAM-PAGELESS]`），
而且 **缺页页号 = 2486 —— 与 `p2e` 崩溃时的 `KeyError: 2486` 逐字相同**。

---

## 1. 【实测】判决表（唯一变量 = 有没有这份补丁）

| 判据 | `p2e`（无补丁，`logs/073`） | ★ **`p2g-fix075`（有补丁）** |
|---|---|---|
| fill 轮 | 16/16 ok | **16/16 ok**（wall 367.5 s） |
| **replay 轮** | ★ **3/16 ok、failed 13** | ★★ **16/16 ok、failed 0**（wall 43.3 s） |
| replay wall | **5.771 s**（= 当场死） | **43.3 s**（正常） |
| `KeyError:` | **16**（8 rank × 2） | ★ **0** |
| `Traceback` | >0 | **0** |
| `EngineDeadError` | 6 | **0** |
| `has not been released` | 32 | **0** |
| HTTP 500 | 有 | **0** |
| `EH0012` | 0 | 0 |
| `[ENGRAM-PAGELESS]` | —（不存在） | ★ **8**（= 8 rank 各一次） |
| 缺页页号 | `KeyError: 2486` | ★★ **2486**（提示原文里） |

⇒ ★★ **机制闭环**：不是"这次没触发所以没崩"，而是 **触发了、被优雅降级、引擎继续跑**。
同一页号出现在两条臂上，说明 `075` 的因果链（命中边界 ⇒ 上一个块缺页 ⇒ 该页号上报）成立。

★ 每次降级 `miss_rows = 3` 行（8 rank 一致 ⇒ 确定性，与 `072` §3.1 的"8/8 同值"同性质）。

---

## 2. ★★★ 【实测】我自己踩的判据污染（写进纪律）

第一版提示文字我写的是：
```
[ENGRAM-PAGELESS] 镜像缺页：第 2486 页 … 已按 barrier 语义用 pad_id=2 填历史并继续（**不再 KeyError**）。
```
⇒ 交付脚本里那条判据 `grep -c 'KeyError' <serve.log>` **把提示自己也数了进去**：
读数是 **`KeyError=8`**（8 个 rank 的提示），看起来像"引擎又炸了 8 次"，而**真异常是 0**。

★ 与 `066c` 的教训同族：**判据与它要测的对象共用了同一个字符串空间**。
**两条修法都做了**：
1. **改措辞**：运行期打印文本里**不得出现裸的 `KeyError` / `Traceback` / `EngineDead` 子串**
   （`engram_hash.py` md5 `240c5a04…` → **`dc63b40b…`**，只改这一处文案，语义未变）
   ⇒ 这一处是 `check_image_fingerprint.sh` 抓到的：它会**把期望 md5 从包里现推**并打印，所以漂移立刻可见；
2. **判据收紧**：真异常一律用带冒号的精确模式 **`grep -ac 'KeyError:'`**（真异常形如 `KeyError: 2486`）。

---

## 3. ⚠️ 仍然没答的（**别把这条判决读成"质量已验"**）

| # | 事项 | 现状 |
|---|---|---|
| 1 | `replay_matches_fill_sha256 = False`、16/16 prompt 都"mismatched" | ★ **这不是保真判据**：replay 用 `--replay-prompt-tokens 65536`、fill 用 131072 ⇒ **输入长度不同**（`072` §2 已明示）。**不能当质量证据，也不能当缺陷证据。** |
| 2 | ★★ **pad 降级的语义影响未量化** | 每个边界 3 行被降级 ⇒ 那几行的 n-gram 历史变成 `pad` ⇒ Engram 注入的 hash 与真值不同。**它到底改不改输出、改多少**，本轮没测。判据 = 同几何下 `VLLM_V41_ENGRAM_TRUE_TOKENS=1`（用真 token 回填）与 pad 版的 **A/B 逐字对比**。 |
| 3 | ★★ **两个残余静默点**（精确修复代理新发现，`docs/ENGRAM-OFFLOAD-EXACT-FIX.md`） | ① 边界落在**页中**时降级发生但 `miss_rows=0` ⇒ **计数器不响**；② **陈旧页**（`present=1`，但那一行是"上一任占用者"的 token）⇒ **静默算错且零计数**。pad 修复**不覆盖**②。 |
| 4 | ★★★ `071` §A1：**取回路径从未被逐字节验证** | 仍然成立。本轮所有证据依旧是"计数器级"（引擎活着 + 降级次数），**没有任何一次是"取回的字节与写下去的字节逐字节相同"**。 |
| 5 | 本轮每臂 1 个样本、`concurrency=1` | 与 `069` 同一局限；`MAX_SEQS`（A2 生产 4）与并发场景未覆盖。 |

---

## 4. 下一步（A3 侧的排序，正在执行）

```
① ★★ TRUE_TOKENS A/B（8 卡 c0）—— 直接量化"pad 降级到底改不改输出"，
    同时用 mode-1 的 mismatch 计数器回答残余静默点 ②（陈旧页）
② ★★★ KV 取回路径的**设备侧逐字节**保真探针（单卡 c1）—— 回答 071 §A1
③ DG0 vs DG1 的 fill sha 差异复现性（`8cd9c27b…` vs `a119d9f6…`，审计 A3 项）
```

★ ① 与 ② 是当前 **A2 上线（`ENGRAM=1` + 卸载）剩下的真正门槛**：① 决定"能不能信这个降级"，
② 决定"卸载这条路本身有没有静默算错"。
