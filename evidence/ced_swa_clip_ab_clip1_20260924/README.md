# SWA-clip 修复的 A 臂（CLIP=1）真机结果：**修复未生效**

日期：2026-09-24 23:03–23:20（Asia/Shanghai）。A3-21 真权重 8+8。
这是 `V41_CED_SWA_CLIP` 修复在 1M 上的**单臂**实测（B 臂 `CLIP=0` 未跑，
因为 A 臂已经没有改善，先保住证据再决定下一步）。

## 条件

- P：chip0–7，`dsv41-ced-trace-p-20260924_trace`，18990
- D：chip8–15，`dsv41-ced-trace-d-20260924_trace`，18991；容器内环境
  已核对为 `V41_CED_SWA_CLIP=1`、`V41_CED_METADATA_INLINE=1`、
  `V41_CED_GRAPH_PROMPT_TAIL_EAGER=1`，`GRAPH=1 EAGER=0`
- proxy：18992
- 请求：同一个 1M 请求（D 收到 `prompt_tokens=1019847`），`temperature=0`、
  非流式、串行 10 次，每次间隔为上一次完成
- 探针：`V41_CED_BLOCK_TRACE=1`、`V41_CED_SWA_TRACE=1`、Engram 直方探针

## 结果

| # | 结果 | completion | g0 first | g0 last | g0 descents | head 方向 |
|---:|---|---:|---:|---:|---:|---|
| 1 | PASS | 7 | 1 | 7968 | 0 | 升 |
| 2 | PASS | 7 | 7990 | 15957 | 0 | 升 |
| 3 | PASS | 7 | 15979 | 23946 | 0 | 升 |
| **4** | **FAIL** | 1 | 23968 | 6136 | **1854** | 升 |
| 5 | PASS | 7 | 6114 | 14125 | 7967 | 降 |
| 6 | PASS | 7 | 14103 | 22114 | 7967 | 降 |
| 7 | PASS | 7 | 22092 | 7968 | 6114 | 降 |
| **8** | **FAIL** | 1 | 30081 | 15957 | **6114** | 降 |
| 9 | PASS | 7 | 1 | 23946 | 1 | 升 |
| 10 | PASS | 7 | 7990 | 6136 | **1854** | 升 |

通过 8/10，失败在 **#4 与 #8**。失败形态与修复前一致：
HTTP 200、`completion_tokens=1`、`content=null`。

## 三条硬结论

1. **裁剪确实执行了。** `[CED-SWA-CLIP]` 按层统计各 **80 行** = 10 请求 × 8 rank
   （layer 2 与 layer 20 各 80），`[CED-SWA-TRACE] phase=replay` 也是 80 行
   = 10 请求 × 8 rank。所以"没生效"不是"没跑到"。
2. **修复没有改善失败。** 失败相位与修复前逐位相同（都是 #4、#8），
   通过率同为 8/10（修复前 8 次里 6/8）。
3. **"块碎片化 ⇒ 失败"被推翻。**
   - #4 与 #10 的 g0 统计**完全相同**（`last=6136`、`descents=1854`），一个 FAIL 一个 PASS；
   - #7 与 #8 共享 `descents=6114`，一个 PASS 一个 FAIL；
   - #5/#6 的 `descents=7967`（比失败的两个都更碎）却都 PASS。

   因此 `descents` / `first` / `last` 这类摘要量**不能**作为判别量。
   之前 4 请求的小样本里"失败的那一个恰好是碎片的"是巧合。

## 仍然成立的结论（本轮新增，未推翻）

- **P→D 传输内容不是判别量**（更早一次：通过-失败配对 `long_kv`/`index_k`
  maxdiff = 0/0/0）。
- **Engram 4-gram 不是判别量**：本批前 4 个请求（含失败的 #4）的 `hash8`
  逐位相同。
- **传输在 replay 之前完成**：失败的 #4 的 8 条 transfer 用时 119.7–135.8 ms，
  同秒内紧接 `[CED-D] replay`，没有"读到未传完的 KV"的迹象。
- 失败仍满足"**需要池回绕**"这一必要条件：144K（每请求 ~1127 块、8 次不绕回）
  8/8 通过、22-token 短针 8/8 通过；1M / 900K 每请求 7989 / 6990 块，
  都在 30082 块的池里发生回绕。

## 下一步该测什么（未执行）

`descents` 无判别力说明"块布局摘要"不够用。下一次实验需要：

1. **打印完整块列表（或全部连续段边界）**，而不是只有 `head/first/last/descents`；
   现有探针的 `head` 只给了前 8 个元素，无法比较 #4 与 #10 的结构差异。
2. **把层探针（`V41_CED_LAYER_SNAPSHOT_POS`）打开**，对失败的 #4 与通过的 #5
   做逐层 digest 对比，定位第一个发散层（修复前那一批显示层 0 一致、层 1 输入已分叉）。
3. **换池大小做单变量**（例如把 D 的 KV 池显式压小/放大），看失败位置是否随池大小
   平移：若平移 ⇒ 与块游标有关；若恒定在 #4/#8 ⇒ 与请求序号/槽位有关。

## 后续追加实验（2026-09-25 00:xx，同一个 D 实例，未重启）

### A. 续发 6 次：失败点正好落在 #12、#16

接着再发 6 个同款 1M 请求（记作 c2_1…c2_6，全局第 11–16 个），结果：

| 全局 # | c2 序号 | 结果 |
|---:|---:|---|
| 11 | c2_1 | PASS |
| **12** | c2_2 | **FAIL** |
| 13 | c2_3 | PASS |
| 14 | c2_4 | PASS |
| 15 | c2_5 | PASS |
| **16** | c2_6 | **FAIL** |

⇒ 连续 16 个 1M 请求里，失败恰为 **#4、#8、#12、#16**（全部 ≡ 0 mod 4），
通过 12/16。失败间隔 #4→#8 与 #12→#16 都是 **6 分 53 秒（413 s）**，
而每个请求耗时 103 s ⇒ 413 ≈ 4 × 103。

### B. 插入 3 个短请求**没有**改变失败相位

在 #16 之后依次发 3 个 22-token 短请求（s1/s2/s3，全部 PASS，耗时 2.3/0.6/0.6 s），
再发第 4 个请求（L1，1M）——**L1 PASS**。

若周期按"全部请求"计数，L1 是全局第 20 个、应当失败；它通过了 ⇒
**短请求（或 ≤128 token、无长 replay 的请求）不推动这个周期**，
周期只按"走完整 128-token replay 的长请求"推进。

### C. 寻址元数据在通过/失败之间**逐字段相同**（原假说出局）

`[CED-SWA-TRACE] phase=replay`（layer 20，rank 0）在 #1…#6 六条
（含失败的 #4、通过的 #5）**完全相同**：

```
layer=20 phase=replay n_queries=128 pos=1019718..1019845 seq_len=1019846
bt_shape=(1, 8192) row_len=8192 row_head=[0, 0, 0, 0]
row_nonzero=[7966, 7967] need_blocks=7965..7967
```

即算子在这一步收到的**地址与长度元数据一字不差**，却一个失败一个通过。
结合"块统计相同的两个请求结果相反"，可以断言：
**根因不在寻址元数据、也不在块布局，而在这些地址背后的内容/时序。**

### D. P 侧延迟释放的超时值

两个容器内 `VLLM_MOONCAKE_ABORT_REQUEST_TIMEOUT = 480`（秒）。
P 全生命周期共 40 条 `Delaying free of 7979 blocks`、8 条 `Force freed`
（全部发生在同一时刻、同一个请求）。413 s 的失败间隔与该 480 s 超时**不等**，
但同为"分钟级"，需要用空闲插入实验区分"计数周期"与"时间周期"（见下）。

### E. 正在跑：空闲插入判别（本轮最后一个实验）

在 #17（L1）之后**先空转 8 分钟**（>480 s），再连发 4 个 1M 请求（i1…i4）：

- **计数假说**：下一个失败仍是第 4 个长请求 → i3（全局第 20 个）失败；
- **时间假说**（≈413–480 s 的定时器/超时）：空转期间定时器在无请求在飞时到期，
  相位被打断 → i1…i4 全部通过。

两者预测**互相矛盾**，因此这一次即可判定周期是"按长请求计数"还是"按墙钟/活跃时间"。

### E 结果：**计数假说成立**（2026-09-25 00:08–00:16）

```
[idle-test] sleep 480 start 2026-09-25T00:00:44+08:00
[idle-test] sleep done  2026-09-25T00:08:44+08:00
i1  PASS   completion=7   wall=103.0s
i2  PASS   completion=7   wall=103.1s
i3  FAIL   completion=1   wall=102.8s   ← 全局第 20 个长请求
```

空转 8 分钟（> `VLLM_MOONCAKE_ABORT_REQUEST_TIMEOUT=480 s`）**没有**重置相位：
第 20 个长请求照旧失败。⇒ **周期由"长请求的个数"推进，与墙钟/超时无关**，
480 s 超时假说出局。

至此已排除的候选（都有实测依据）：

| 候选 | 结论 |
|---|---|
| D 侧物理块布局（碎片度/首末页/整行） | 不是判别量（#4 与 #10 布局统计相同、结果相反） |
| 算子收到的寻址元数据 | 不是判别量（通过/失败逐字段相同） |
| P→D 传输内容（尾位置 long_kv/index_k） | 不是判别量（通过-失败配对 maxdiff=0） |
| Engram 4-gram / hash 值 | 不是判别量（含失败的 #4 在内逐位相同） |
| 墙钟/超时机制（480 s） | 空转 8 分钟后相位不变 ⇒ 排除 |
| 短请求（≤128 replay） | 不推进周期（插 3 个短请求后失败点仍按长请求计数） |

**剩余最强候选：D 的请求槽位/批内常驻状态**（周期 = `MAX_SEQS = 4`）。
下一步单变量：把 D 改成 `MAX_SEQS=1`，其余完全不变，跑 8 个长请求——
若失败消失 ⇒ 槽位/批内 buffer 相关；若仍每 4 个失败 ⇒ "4" 与 `MAX_SEQS` 无关。

### F. `MAX_SEQS=1` 单变量：**周期不变**（2026-09-25 00:22–00:36）

重启 D，只把 `MAX_SEQS` 从 4 改成 1（其余完全不变：同样的 `GRAPH=1 EAGER=0`、
prompt-tail eager、metadata inline、`V41_CED_SWA_CLIP=1`）。新实例、
`num_blocks: 30200`（标准臂是 30082）、`max-num-seqs 1` 已在容器命令行核对。

```
ms1_1  PASS  completion=7
ms1_2  PASS  completion=7
ms1_3  PASS  completion=7
ms1_4  FAIL  completion=1   ← 仍是第 4 个
ms1_5  PASS  completion=7
ms1_6  PASS  completion=7
ms1_7  PASS  completion=7
ms1_8  FAIL  completion=1   ← 仍是第 8 个
```

⇒ **周期 4 与 `MAX_SEQS` 无关**（也不是某个具体 D 实例的残留状态：
这是全新实例）。前面把"周期 = MAX_SEQS"当成最强候选的判断被这次实验否掉。

同时要修正本文早先一处过强的表述：我们只证明了块布局的**摘要**
（`n`/`first`/`last`/`descents`）不判别，而**摘要相同的两个列表顺序可以完全不同**，
所以"回收/绕回"这一类机制**并未被排除**。
两个 D 实例的池/请求比都落在同一档：
`30082 / 7989 = 3.766`（标准臂）、`30200 / 7989 = 3.780`（MAX_SEQS=1）⇒
`ceil(比值) = 4`，与观测周期一致。

下一步单变量：**把 KV 池压到"约 2.4 个请求"的量**（`--kv-cache-memory-bytes`），
让比值落到 `ceil = 3`。若失败周期随之变成 3 ⇒ 与池的绕回/回收直接相关；
若仍是 4 ⇒ 与池无关，回到"按请求计数"的其他机制。

### G. 压小 KV 池：**4 个一循环的失败消失**（2026-09-25 00:46–00:57）

保持 `MAX_SEQS=1`，只把 D 的 KV 池显式压到 `--kv-cache-memory-bytes=10545000000`
（实测 `num_blocks: 19494`，`19494 / 7989 = 2.44`），其余完全不变。连发 6 个 1M：

```
sp_1 PASS 102.7s | sp_2 PASS 102.7s | sp_3 PASS 102.7s
sp_4 PASS 102.7s | sp_5 PASS 102.7s | sp_6 PASS 102.7s
```

**6/6 全过** —— 这是本次调查里第一次连续 6 个 1M 请求没有出现偶发失败
（标准臂最好成绩是连续 3 个）。另外注意 wall 时间也变得整齐（全部 102.7 s，
标准臂是 103.0–103.2 s）。

**但周期并没有变成 3**（`sp_3` 与 `sp_6` 都通过），所以"失败发生在绕回的那一个
请求上"这个具体形式**不成立**：按 FIFO 前进，这个池下第 3、5 个请求就会绕回，
而它们都通过了。⇒ 触发条件与**池几何**有关，但不是"绕回"这一简单事件。

三次单变量对照（全部同一 prompt、串行、`temperature=0`）：

| D 配置 | `num_blocks` | 池/请求 | 失败位置 |
|---|---:|---:|---|
| `MAX_SEQS=4`（标准臂） | 30082 | 3.77 | #4, #8, #12, #16 |
| `MAX_SEQS=1` | 30200 | 3.78 | #4, #8 |
| `MAX_SEQS=1` + 小池 | 19494 | 2.44 | 0（本表只跑到第 6 个） |

这是目前**唯一**能稳定跑过 4 个连续 1M 请求的配置，因此既是重要线索，也是
一个（尚未验证充分、机制不明）的**候选缓解手段**。必须先用更多请求确认它不是
更长周期（例如 7 或 8）的假象，再谈缓解。

#### G-1. 追加 6 个请求：**12/12 全过**

紧接着再发 6 个同款 1M（`sp2_1…sp2_6`），全部通过：

```
sp2_1 PASS 102.6s | sp2_2 PASS 102.7s | sp2_3 PASS 102.6s
sp2_4 PASS 102.6s | sp2_5 PASS 102.8s | sp2_6 PASS 102.6s
```

累加第一轮，**小池配置连续 12 个 1M 请求全过**；而标准池在同一台机器、
同一 P、同一请求下是**每 4 个必失败**（16 个里失败 4 个，位置 4/8/12/16）。
这不是"更长周期的假象"——若周期是 7 或 8，12 个里至少会命中一次。

**注意当前可交付性边界**：这一轮小池实验同时改了 `MAX_SEQS=1`（因为它是紧接
上一节实验的继续）。因此"小池"与"`MAX_SEQS=1`"这两个变量还没有分开验证。
下一步必须做 **`MAX_SEQS=4` + 小池**：若同样 12/12 ⇒ 小池是决定性因素，
可以恢复标准臂的 `MAX_SEQS=4`；若又出现每 4 个失败 ⇒ 是两个变量的组合效应。

### H. 变量分离：**`MAX_SEQS=4` + 小池 = 12/12 全过**（2026-09-25 01:17–01:38）

重启 D，把 `MAX_SEQS` 恢复成 **4**（与标准臂一致），只保留小池：

```
sp4_1 … sp4_12  PASS   completion=7   wall=102.9–107.2 s
```

配置已从两处核对：D 日志 `max-num-seqs 4`、`num_blocks: 19494`；容器环境
`V41_CED_SWA_CLIP=1`。

⇒ **小池是决定性因素，`MAX_SEQS` 无关**。四组对照（同一 prompt、串行、
`temperature=0`、`GRAPH=1 EAGER=0`、`CLIP=1`）：

| D 配置 | `num_blocks` | 池/请求 | 12 个长请求里的失败 |
|---|---:|---:|---|
| `MAX_SEQS=4`（标准） | 30082 | 3.77 | 4 个（#4/#8/#12/#16） |
| `MAX_SEQS=1` | 30200 | 3.78 | 2 个（#4/#8） |
| `MAX_SEQS=1` + 小池 | 19494 | 2.44 | **0** |
| `MAX_SEQS=4` + 小池 | 19494 | 2.44 | **0** |

**下一步（已脚本化，未执行）**：判别量可能是"**D 的池是否大于 P 的池**"，
而不是池的绝对大小。**P 的池当前是 29721 块**（注意：本文早先按更早的 P 实例
写过 30079，那是陈旧值；以 29721 为准，见 `docs/CED-PD-PAUSE-20260925.md`）。
标准臂 D=30082 比 P 多 **361** 块，小池 19494 远小于 P。
用 `tools/ced_pool_threshold_experiment.sh` 跑两个臂：
D≈29000（**小于** P）应全过；D≈30500（**大于** P）应每 4 个失败。

### I. 池阈值实验：A 臂成立，但 **B 臂是无效数据**（2026-09-25 01:43–02:16）

**有效结果**：`below` 臂 D=**29024**（< P 29721），**8/8 通过**。

**B 臂（`above`）没有跑起来，它的 8 条 "通过" 是假象。** 事后核对：

```
docker ps -a  →  dsv41-ced-trace-d-above: No such object   （容器从未创建）
docker inspect dsv41-ced-trace-d-below → StartedAt=2026-09-24T17:43:32Z（01:43 CST，一直存活）
pt_above_launch.log → serve_a3.sh 报"选中的卡里有正在被占用的"，拒绝启动
```

机制：脚本只 `docker rm` 了**上一臂**的容器名，没删更早的 `below`；而就绪检查只是
`curl 18991/health` —— **旧容器替新容器回答了健康检查**。于是 `above_1..8` 全部
发给了仍在运行的 `below` 实例。`above_6/7/8` 的"通过"只是 `below` 序列的第
14–16 个请求。

⇒ 因此**把 `below` 的有效样本数合并计算：D=29024（< P）连续 16/16 通过**。

> 这个 bug 本身和本次调查的主线是同族问题：**静默降级**（失败的前置条件不报错、
> 由旧状态顶上）。修正版脚本 `tools/ced_pool_arms_experiment.sh` 因此加了四道硬门：
> ① 启动前删除**所有** `dsv41-ced-trace-d*` 并等 `VLLMWorker` 归零；
> ② 就绪必须同时满足"新容器在运行"；
> ③ 新容器的 `serve.log` 必须存在且 mtime 晚于本次启动时刻；
> ④ 该日志里必须已打印 `num_blocks`。任一不满足即 `FAIL` 退出，不发请求。

### J. 决定性双臂（已启动，2026-09-25 12:13）

`C>P` 与 `C≈30k` 之前是混淆的。修正版脚本在 P=29721 两侧各取一个**尽量贴近**的点：

| 臂 | 目标块数 | 与 P 的关系 | 预测（若"D>P"成立） |
|---|---:|---|---|
| `hi` | ≈29850 | D **>** P（+129） | 每 4 个长请求失败 |
| `lo` | ≈29600 | D **<** P（−121） | 全过 |

两点只差约 250 块，可把"关系"与"绝对大小"分开。脚本同时打开
`V41_CED_BLOCK_DUMP_DIR`，把每个请求的**完整 g0 块列表**落盘。

判读要点（恢复时）：
- 若 `hi` 失败且 `lo` 全过 ⇒ **D>P 关系确认**，转去查两侧 `num_blocks` 参与的计算。
- 若 `hi` 也全过 ⇒ 关系假说否掉；用完整块列表找"失败请求是否触及某个 id 区间"。

**为此需要的额外一步**：远端 shadow 包原本**没有** block-dump 代码（只在本地仓库里），
现已同步 `experimental/ced/mooncake_hybrid_connector.py` 并用
`tools/patch_trace_env.py` 补上 `V41_CED_BLOCK_DUMP_DIR` 透传，实测
`grep -c _ced_block_dump` = 2、`grep -c V41_CED_BLOCK_DUMP_DIR` = 1。

### K. **决定性结果：失败请求正是第一个块号触及 P 池范围的请求**（2026-09-25 12:23–）

`hi` 臂四道硬门全过：新容器在运行、`serve.log` mtime 晚于启动时刻、
`num_blocks: 29850`、`max-num-seqs 4`。**D = 29850，仅比 P 的 29721 多 129 块。**

结果：`hi_1`/`hi_2`/`hi_3` **PASS**，**`hi_4` FAIL**（第 4 个，与标准臂同位置）。

打开完整 g0 块列表（`[CED-BLOCK-DUMP]` 落盘）后，判别量一目了然：

| # | 块号范围 | 落在 `[29721, 29850)` 的块数 | 结果 |
|---:|---|---:|---|
| 1 | 1 … 7968 | **0** | PASS |
| 2 | 7990 … 15957 | **0** | PASS |
| 3 | 15979 … 23946 | **0** | PASS |
| **4** | **5904 … 29849** | **129** | **FAIL** |

**129 = 29850 − 29721**，也就是"D 有、P 没有"的块号**一个不落全被这个请求摸到了**。
前三个请求的块号最大值 23946 < 29721，**从未进入该区间**，全部通过。

⇒ 至此"块布局摘要不判别"与"寻址元数据不判别"**并不矛盾**：那些探针看的是
**摘要**（`first/last/descents/head`）与**长度**，而这个判据是"**块号是否越过 P 的池**"，
摘要恰好对此不敏感（同一摘要可以既越界又不越界）。

### L. 五臂汇总（同一 1M 请求、串行、MAX_SEQS=4、CLIP=1）

| D 池 | 与 P(29721) 关系 | 首个触及 ≥P 的请求 | 结果 |
|---:|---|---|---|
| 30200 | +479 | #4 | 每 4 个失败 |
| 30082 | +361 | #4 | 每 4 个失败（#4/#8/#12/#16） |
| **29850** | **+129** | **#4** | **#4 失败** |
| 29024 | −697 | 从不 | 16/16 通过 |
| 19494 | −10227 | 从不 | 12/12 通过 |

规律：**D 的池一旦大于 P 的池，那个跨过 P 边界的请求就失败**；小于则从不失败。
但"D>P"与"D 绝对大小 ≈30k"在现有数据里仍然混淆——**下一步必须换 P 的池**来分开
（把 P 调大让 P>D，若 D 转为通过 ⇒ 关系确认）。

### M. `hi` 臂完整 8 次：**失败在 #4 与 #8**

```
hi_1 PASS  hi_2 PASS  hi_3 PASS  hi_4 FAIL
hi_5 PASS  hi_6 PASS  hi_7 PASS  hi_8 FAIL
```

即 D=29850（>P）复现了与标准臂（30082）**完全相同的每 4 个长请求失败**。

### N. `lo` 臂（D=29600 < P）的判读价值

`lo` 设计成**只比 P 小 121 块**，它的结果能把阈值钉在很窄的区间里：

| `lo` 结果 | 推出的阈值位置 | 含义 |
|---|---|---|
| **通过** | 阈值 ∈ (29600, 29850]，而 **P=29721 正落在其中** | 强烈指向"阈值就是 P 的池" |
| **失败** | 阈值 ∈ (29024, 29600]，**P=29721 在阈值之上** | ⇒ 与 P 无关，是 D 的绝对大小（某个 ≈29.2–29.6k 的常数） |

6 个有效点汇总（同一 1M 请求、串行、`MAX_SEQS=4`、`CLIP=1`、P=29721）：

| D 池 | 与 P 关系 | 首个越界请求 | 结果 |
|---:|---|---|---|
| 30200 | +479 | #4 | #4 失败 |
| 30082 | +361 | #4 | #4/#8/#12/#16 失败 |
| **29850** | **+129** | **#4** | **#4/#8 失败** |
| 29600 | −121 | ? | 进行中（决定性） |
| 29024 | −697 | 从不 | 16/16 通过 |
| 19494 | −10227 | 从不 | 12/12 通过 |

### O. 因果实验（脚本已就绪，待 `lo` 跑完执行）

即便 `lo` 通过，仍不能排除"D 的绝对阈值恰好落在 P 的数值附近"。唯一能证明
**是关系**的办法是**移动 P 的池**：`tools/ced_p_enlarge_experiment.sh`
把 P 调到 ≈30500 块（> D 的 29850），其余（`MAX_LEN/MAX_SEQS/BAT_TOKENS/
GPU_UTIL/BLOCK/KV_DTYPE/ENGRAM/MULTISTREAM/DSA_OVERLAP`）与当前 P 逐项一致
（已对照 P 的 `inner.sh`）。

- D 转为通过 ⇒ 关系因果成立，转向查两侧 `num_blocks` 参与的计算；
- D 仍失败 ⇒ 与 P 无关，是 D 自身在某个 ≈29k 阈值上的问题。

### P. 机制层面已排除的一条

通读传输路径后确认：**连接器里 D 的块号只索引 D 自己的地址、P 的块号只索引 P 的
地址**（`src = local_addrs + local_block_id*stride`、
`dst = remote_addrs + remote_block_id*stride`），且对端 `num_blocks`
**从未被读取**（只用于自家注册长度与握手字段）。所以这不是"块号被拿去索引对端缓冲"
那类越界；机制在更下游（传输引擎注册窗口、或 KV 张量/分配器），
需要靠 O 的因果实验与后续探针定位。
该脚本同时打开 `V41_CED_BLOCK_DUMP_DIR`，把每个请求的**完整 g0 块列表**落盘
——因为摘要（`first/last/descents`）已被证明无法区分顺序不同的列表。

## 文件

- `g0_stats.tsv`：上表的机器可读版本（由运行时日志摘出）
- `SHA256SUMS`：校验和

> 原始 serve.log 与逐请求响应仍在 A3-21：
> `/home/l00886679/tmp/20260924/ced_numeric/pkg_d7953e5_trace/results/ced_trace_d_20260924_trace/`
> （`serve.log`、`probe/clip1_seq10.log`、`probe/clip1_seq10/`）。未同步到本地。
